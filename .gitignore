# macOS metadata
.DS_Store
._*

# Nix / direnv generated state and root build-result links
.direnv/
/result
/result-*
/marketing/.firebase/

# Python generated files and local environments
__pycache__/
*.py[cod]
.venv/

# Xcode / SwiftPM build products and per-user settings
/build/
/artifacts/
DerivedData/
.build/
xcuserdata/
*.xcuserstate
*.xcarchive/
*.xcresult/
*.ipa
*.dSYM/
*.dSYM.zip

# Local environment values; share only example/template files
.env
.env.*
!.env.example
!.env.*.example
!.env.template
!.env.*.template

# Local signing credentials and provisioning profiles
.appstoreconnect/
nibble.env
AuthKey_*
*.p8
*.p12
*.pfx
*.mobileprovision
*.provisionprofile

# Editor temporary files
*.swp
*.swo
*~

# Keep flake.lock, Package.resolved, Xcode project/shared settings,
# and UI screenshots/recordings eligible for version control.
