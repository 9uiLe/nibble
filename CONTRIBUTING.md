# 開発ガイド

## プロダクトの判断基準

nibble は iOS 向けのスニペットツール。作業中に必要なスニペットを素早く呼び出せ、作成・編集・削除を迷わず行えることを価値とする。

機能の多さより、目的を達成するまでの操作の少なさ、理解しやすさ、入力や描画を待たせないことを優先する。ユーザー体験・UI/UX と性能は、仕上げの作業ではなく設計・実装・レビューの受け入れ条件にする。

「あらゆるシーンから呼び出す」はプロダクトの目標。各導線の設計前に iOS の公開 API、権限、extension の制約を確認し、提供できる体験を実機で確かめる。

## 対応 OS

- 最低対応バージョンは **iOS 26.0** とする。
- アプリと関連する iOS extension の deployment target は `26.0` にそろえる。Swift Package がある場合も iOS の最低対応条件を整合させる。
- iOS 26.0 より新しい API を使う場合は availability を扱い、26.0 で主要な体験を維持する。
- iOS 26.0 と、採用する Xcode が対応する最新の正式版 iOS で影響範囲を検証する。ベータ版だけでの確認を完了条件にしない。
- 最低対応 OS の引き上げは、対象ユーザーへの影響と理由を記録し、独立した変更としてレビューする。

現在は Xcode プロジェクトがないため、deployment target はまだ設定されていない。プロジェクト作成時にこの条件を反映し、Xcode・Swift の採用バージョン、scheme、実行コマンドをこのガイドに追記する。

## 開発ツールの管理

開発・検証に必要な補助ツールは [flake.nix](flake.nix) に宣言し、[flake.lock](flake.lock) をコミットして固定する。ローカルと CI は同じ lock を使う。Homebrew や pip によるツールの個別導入を開発手順の前提にしない。

現時点では以下を提供する。アプリ用のツールが必要になったら、用途と macOS / Linux の対応範囲を確認して flake に追加する。

| ツール | 用途 |
| --- | --- |
| Python 3 + PyYAML | workflow の runner 方針を検査 |
| actionlint + ShellCheck | GitHub Actions の構文・式・埋め込みシェルを検査 |
| nixfmt（`nix fmt` 経由） | Nix 定義を整形 |

nixpkgs は安定版 `nixos-26.05` を入力とし、実際に使用する revision と依存は lock で固定する。Apple Silicon / Intel の macOS と、ARM64 / x86_64 の Linux を対象にする。GitHub Actions の runner は引き続き `ubuntu-24.04` のみ。

nixpkgs の Intel Mac 対応は 26.05 が最終版のため、次の安定版へ更新する際には開発環境の対応範囲も見直す。

### 導入と実行

前提は Git と、`nix-command` / `flakes` が利用できる Nix。Nix 自体はホストに導入する（[公式の導入案内](https://nixos.org/download/)）。機能が無効な環境では、利用者の Nix 設定 `~/.config/nix/nix.conf` に `experimental-features = nix-command flakes` を既存設定と統合して有効にする。

リポジトリルートで実行する。

```sh
# 補助ツールが利用できる開発シェルに入る（終了は exit）
nix develop

# CI と同じ検証。開発シェルの外からも実行できる
nix flake check --no-update-lock-file --print-build-logs
```

個別の操作は次の通り。

```sh
nix develop --command python3 scripts/check_workflows.py
nix fmt flake.nix
```

初回は依存の取得が発生する。flake が参照する新しいファイルは Git の追跡対象に追加する。未追跡のファイルは Git 管理の flake の入力に含まれない。

### 依存の追加・更新

- ツールの追加は `flake.nix` に行う。Python ライブラリも `python3.withPackages` で宣言し、別の requirements ファイルや仮想環境で重複管理しない。
- nixpkgs の更新は `nix flake update nixpkgs` で行い、lock の差分、ツールの互換性、macOS と Linux の検証結果を PR に記録する。通常の CI では lock を更新しない。
- Nix 定義を変更したら `nix fmt flake.nix` と共通の検証コマンドを実行する。更新は単一の目的の PR とし、互換性に問題があれば定義と lock を一緒に戻せる状態にする。

Xcode、Apple の Swift ツールチェーン、iOS SDK、Simulator runtime、署名情報はローカル Mac 側で管理する。Nix の開発シェルは `mkShellNoCC` を使用し、別のコンパイラを追加しない。iOS ビルドは選択した Xcode のツールチェーンを使用し、そのバージョンを検証結果に記録する。

## ローカルとクラウドの責務

| 検証項目 | ローカル Mac / Xcode / 接続した実機 | クラウド / GitHub Actions |
| --- | --- | --- |
| Workflow の runner 方針 | CI と同じスクリプトで事前確認 | Ubuntu で必須検査 |
| 静的検査・文書・依存関係 | 変更に応じて確認 | Linux で動作する検査を導入時に実行 |
| iOS アプリ・extension のビルド、署名 | 担当。変更したターゲットを確認 | 実行しない |
| Apple SDK を必要とする単体・統合・UI テスト | 担当。変更の影響に応じて実行 | 実行しない |
| 見た目、操作、アクセシビリティ | Simulator / 実機で操作し、画像・動画を確認 | 実行しない。PR の証跡をレビューする |
| 起動、入力応答、描画、メモリ等の性能 | Release 構成を実機で測定。必要に応じて Instruments を使用 | iOS の性能を判定しない |
| OS や他アプリとの連携、実機固有の挙動 | 公開 API の制約を含め実機で確認 | 実行しない |

### GitHub Actions の制約

- **macOS runner の使用を禁止する。** GitHub-hosted、self-hosted、再利用 workflow 経由を含む。iOS 検証を目的とした例外も設けない。
- 各ジョブは GitHub-hosted の `runs-on: ubuntu-24.04` を直接指定する。
- runner の式・matrix による動的選択、runner group、self-hosted runner、ジョブ単位の再利用 workflow 呼び出しは使用しない。runner を監査可能に保つための制約とする。runner 以外の matrix は使用できる。
- Action やスクリプトから別の macOS ジョブを起動する迂回も禁止する。
- 外部 Action はコミット SHA に固定し、更新時に実行内容と互換性を確認する。
- `Workflow policy / workflow-policy` は Nix で固定した依存を使い、runner 方針、workflow の構文・シェル、Nix 定義の書式を検査する。iOS のビルドやテストを保証しない。
- この検査は workflow 起動前に runner を遮断する仕組みではない。workflow の変更は差分でも必ず確認する。GitHub のブランチ保護を設定する際は、このチェックを必須にする。

ローカルと CI の共通コマンドは `nix flake check --no-update-lock-file --print-build-logs`。導入・更新方法は [開発ツールの管理](#開発ツールの管理) を参照する。

クラウド環境だけで作業した場合、必要なローカル検証を未実施として明記する。ローカルで検証できる開発者が対象コミットで確認し、結果と証跡がそろうまでマージしない。

## UI/UX と性能の確認

UI/UX に影響する変更は、内部実装のみの変更であっても、対象導線を操作し、スクリーンショットや画面録画を取得して内容を確認する。コードや Preview を見るだけで完了としない。

- 見た目の変更は、変更前後のスクリーンショットを PR に添付する。新規画面なら変更後と到達手順を示し、変更前がない理由を書く。
- 呼び出し、入力、作成・編集・削除、画面遷移、キーボード、アニメーション、待ち時間など、操作や時間経過を伴う変更は画面録画も添付する。
- 画像・動画には対象コミット、端末 / Simulator、iOS バージョン、再現手順を添える。PR に直接アップロードするか、レビュー担当者が閲覧できる保存先を使用する。
- 文書や CI 設定だけの変更も PR の証跡欄は残し、「対象外」と UI/UX に影響しない理由を書く。画像・動画を用意できない UI/UX 変更を対象外として扱わない。
- 証跡にはダミーのスニペットを使い、個人情報や秘密情報を写さない。

変更の影響に応じて次の観点を確認し、PR に結果を記録する。

| 観点 | 確認内容 |
| --- | --- |
| 基本の導線 | 呼び出しから利用までの操作数、作成・編集・削除、閉じる操作、作業への復帰 |
| 状態 | 空・少量・大量のデータ、長文、検索結果なし、エラー、連打・繰り返し操作、削除時の誤操作への配慮 |
| 入力と表示 | 日本語の変換・確定、フォーカス、キーボード表示、小さい画面、大きな文字、ライト / ダーク |
| アクセシビリティ | VoiceOver の読み上げと順序、操作領域、コントラスト、Dynamic Type、視差効果を減らす設定 |
| 応答と描画 | 起動と再呼び出し、入力の追従、検索、保存、スクロール、画面遷移の待ち時間や引っかかり |

性能に関係する変更では、同じ実機・OS・ビルド構成・データ件数・操作条件で変更前後を比較する。起動 / 呼び出し時間、入力から表示までの遅延、スクロールの hitch、メモリなど、対象の体験に対応する指標を選ぶ。測定回数と集計方法も残し、体感や動画だけで性能改善を断定しない。

各導線を初めて実装する際に基準値を測定し、受け入れ可能な応答時間やデータ件数の性能予算を決めて記録する。メインスレッドの重い処理、不要な再描画、全件読み込みなどは測定に基づいて見直す。性能の悪化があれば原因と対策を示し、合意した基準を満たすまで完了としない。

## 技術の選定

新しい技術を積極的に検討しつつ、採用の目的はプロダクトの体験と継続的な開発を支えることとする。新しさだけを採用理由にしない。

Swift / SwiftUI、Observation、Swift Concurrency、Swift Testing などの現行の標準技術を候補にし、必要なら UIKit 等も比較する。保存方式や App Intents、Share Extension、キーボード等の呼び出し手段は、利用シーンと OS 制約を検証して選ぶ。これらの列挙は採用決定ではない。

主要な技術、永続化、外部依存、呼び出し導線など、後から変更する負担が大きい選択は、小さな検証を行い、次の内容を `docs/decisions/NNNN-短い名前.md` に記録する。ディレクトリは最初の決定時に作成する。

1. 解決したい課題と、期待するユーザーの体験。
2. 比較した候補、採用する案、採用しない案の理由。
3. iOS 26.0 / extension / 公開 API への適合性と、必要な権限。
4. 描画・応答速度、メモリ、バッテリー、データ量への影響と検証結果。
5. API の成熟度、テスト容易性、依存の維持状況、ライセンス、保守・運用負荷。
6. データ保護、移行・互換性、採用案を置き換える手段。
7. 決定日、状態（提案 / 採用 / 廃止）、見直す条件、関連 PR と証跡。

外部依存は、標準 API や小さな実装との比較を含めて判断する。データの読み書きを変える場合は、既存スニペットを失わず移行できることを確認する。

## PR の作り方と完了条件

- 1 PR は **小さく、単一の目的** にする。機能変更に無関係な整形・リファクタリング・依存更新を混ぜない。
- 行数の上限ではなく、1 つのアウトカムを独立して理解・検証・差し戻しできることを基準にする。大きくなる場合は、利用可能な小さな変更に分割する。
- [.github/pull_request_template.md](.github/pull_request_template.md) の各欄を埋める。目的・背景、アウトカム、変更内容のコミット表、スクリーンショット・画面録画は必須。
- 変更内容の表には PR に含む全コミットの実際のハッシュと説明を書く。ハッシュはリンクまたは識別可能な短縮形を使う。rebase、追加コミットなどの後は表も更新する。
- アウトカムには、変更によって利用者または開発者に何が可能になるかと、その達成を確認する条件を書く。
- 検証結果には対象コミット、環境、実行したコマンド / 手順と結果を書く。未実施を成功として扱わない。
- UI/UX と性能に影響する追加修正をしたら、修正後の状態で検証し証跡を更新する。証跡取得後に関連しない変更のみを行った場合は、その旨を記録する。
- マージ前に、CI の成功、変更に必要なローカル検証、レビュー可能な証跡、PR 本文と最終差分の一致を確認する。
