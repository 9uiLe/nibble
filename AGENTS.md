# エージェント向け開発ルール

作業前に [CONTRIBUTING.md](CONTRIBUTING.md) を読み、実装・検証・PR 作成に適用する。
このファイルは要点と入口であり、詳細な基準は開発ガイドを正とする。

- nibble は iOS 26.0 以上向けのスニペットツール。使いやすさ、シンプルさ、描画・応答性能を優先する。
- 変更を小さく保ち、1 PR を単一の目的にする。無関係なリファクタリングや依存更新を混ぜない。
- 開発・検証の補助ツールは `flake.nix` で宣言し、`flake.lock` で固定する。ローカルと CI は同じ Nix 環境を使い、Homebrew・pip 等の別管理を前提にしない。
- 共通の検証は `nix flake check --no-update-lock-file --print-build-logs`。Xcode・iOS SDK・Simulator はローカルの Apple 配布物を使う。
- GitHub Actions で macOS runner を使用しない。self-hosted の Mac や再利用 workflow 経由も禁止。
- GitHub Actions の各ジョブは `runs-on: ubuntu-24.04` を直接指定する。
- iOS のビルド・テスト・UI/UX・性能確認はローカル Mac が担当する。クラウドの静的検査だけで検証済みとしない。
- UI/UX を変更したらスクリーンショット・画面録画で実際の挙動を確認し、PR に添付する。操作・遷移・応答が変わる場合は動画も必要。
- 画像や動画は PR のレビュー担当者が閲覧できる形で添付する。ローカルファイルのパスだけでは完了としない。
- 実行していない検証や、取得していない証跡を実施済みとして報告しない。環境がなければ未実施項目と理由を明記し、レビュー可能な変更まで進める。
- PR には目的・背景、アウトカム、コミットハッシュと説明の表、スクリーンショット・画面録画、検証結果を記載する。対象外の項目も欄を残し、理由を書く。
- 最新技術は候補として評価し、OS/API 制約、性能、保守・運用、移行容易性を根拠に採否を決める。未決定の構成を既定として扱わない。
- 新しいアプリ・extension ターゲットには iOS deployment target 26.0 を設定する。最低対応 OS を変更する場合は、影響を明記した別の意思決定が必要。

このリポジトリには、まだアプリや Xcode の scheme がない。存在しないビルド・テストコマンドを推測で追加せず、プロジェクト作成時に実際に使える手順を開発ガイドへ記載する。
