# スニペットキーボードの検証

[キーボード設計](decisions/0005-snippet-keyboard.md)に対する2026-09-18の実行記録である。再現する操作は[製品手順](mvp.md#キーボードの操作検証)、署名・送信は[配布の検証記録](testflight-validation.md#キーボードを含む内部配布)を参照する。

## 標準キーボードに合わせた外観

対象ソースは`c07902e25d5490ee4b1a1d78c63d15780a27e847`。2026-09-18に、以下の環境と同じiPhone 17 Pro / iOS 26.5 SimulatorでReleaseのUI run `20260918T140332Z-keyboard-ui-f0351c`を実行した。開始・終了時のiOS入力と対象commit、媒体hashは`check_evidence.py --integrity-only`で一致した。

Release製品テストのrun `20260918T142558Z-test-bf9030`は82件、パラメーター展開後94件が成功し、失敗・skipは0件だった。Nix共通検査7件も成功した。モデル・保存・権限処理は変更しておらず、既存の競合・取消・原文保持のテストを回帰確認に使った。

| 確認 | 観測 |
| --- | --- |
| 標準キーボードとの比較 | 同じVerificationAppの入力欄でEnglishキーボードと切り替えた。keyboardスタイルの背景がOSの周囲と連続し、本体のクリーム色の面がない |
| 余白と選択 | 左右8ptを基準に文字と区切り線を揃えた。最大260ptの標準segmented Pickerで「すべて／ピン留め」の選択面を確認。更新・コピー・下部操作が重ならない |
| 配色と向き | 縦のライト／ダーク、Safariの横向きのライト／ダークを原本PNGで確認。横向きでは2行と上部・下部操作が収まる |
| 権限なしの操作 | 直接挿入した本文を入力先でコピーし、保存済み本文66 byteとUTF-8完全一致。キーボードのコピー操作はsentinelを保持し、設定案内を表示 |
| 権限ありの操作 | iOS設定の許可switchがオンであることを読み取り、コピーした66 byteが完全一致。入力欄は空のまま。許可の変更にアイコンも追従 |
| 終了時 | フルアクセスをオフ、外観をライトへ戻した。検証用アプリの縦表示を確認 |

標準キーボードとの比較、縦横・配色、各操作結果のPNG9枚と、6.882秒の録画を保存した。録画は0、3.650、5.937秒の抽出フレームを開き、一覧・挿入結果・コピー権限案内を確認した。フィルターの選択面は原本PNGで確認した。全編再生と外部添付は未実施。実際の媒体名・観測・時刻はrunの`review.json`に記録する。

横向きの入力先はlocalhostで配信した`validation/HostForm.html`である。sim-useの画面情報は874×402、原本PNGはSimulatorの物理方向で保存され、内容が90度回転している。Orcaの回転操作は古い要素番号として拒否され、その呼出しの成功は主張しない。Safariの画面情報と撮影結果に基づいて横向きの成立を確認した。補助操作ログはrun内に保存した。

小さい幅、VoiceOver音声、入力先が明示するkeyboard appearance、実機、描画性能はこのrunでは未検証。本体の標準UI driverと配布はこの外観変更に対して再実行していない。以下の初期統合時の記録は対象ソースが異なり、現在の見た目や配布済みを示すものではない。

[視覚的な契約](decisions/0005-snippet-keyboard.md#レイアウトと操作の意味)の部品・配色・寸法・状態表示をコードと原本画像へ照合した。選択面、補助情報の階層、権限を示す鍵、無効なページ操作、結果文の役割を区別する。標準部品の採用はAppleの指針に基づき、幅・余白・情報量は製品判断として記録した。誤操作率や認知負荷は測定しておらず、利用者評価を要する。

## 初期統合の結果

| 評価 | 確認できた範囲 |
| --- | --- |
| 自動検査 | Release製品テスト82件、パラメーター展開後94件が成功。Nix共通検査7件が成功 |
| キーボード | iOS 26.5 Simulatorで、権限なしの挿入・コピー拒否、許可ありのコピー、原文、フィルター・更新、縦横・配色・標準キーボードへの復帰を確認 |
| 本体の回帰 | 作成・下書き・検索・編集・コピー・整理・操作位置・編集中の背景復帰を標準driverで確認 |
| 配布 | 3 bundleを含む署名済みIPAの生成と、0.1.0 (202609181319)のアップロードが成功 |
| 未確認 | Apple側の処理・グループ反映、実機操作・性能・ロック中の保護、VoiceOver音声、secure入力への切替、小さい幅、実画面での50件超のページ移動 |

媒体はローカル保存で、画像と指定時刻の抽出フレームを確認した。録画の全編再生、外部への添付、レビュー担当者の閲覧は未実施。以下の成功は記載したソース・環境・操作に限定する。

## 環境と対象ソース

- 2026-09-18、Xcode 26.5（17F42）、Swift 6.3.2、macOS 26.2 arm64。
- iPhone 17 Pro、iOS 26.5（23F77）。専用Simulator `D099A849-386F-4EAE-AE12-02D8DC623AF2`。
- 最低対応OS 26.0。署名はSimulator用ad hoc。Apple Developerの認証設定・秘密鍵は参照していない。
- 製品ソースは`1839313261f4200e8502b8dd589c662cb4d2143d`。以下の3 runについて、開始・終了時のiOS入力ファイルがこのcommitと一致し、媒体hashが変わっていないことを`check_evidence.py --integrity-only`で確認した。設計文書の更新とiOSの再実行は区別する。

| run | 確認範囲 | 結果 |
| --- | --- | --- |
| `20260918T115834Z-test-15cc28` | Releaseの全製品テスト | 成功・ソース照合済み |
| `20260918T114010Z-keyboard-ui-54ce58` | Releaseのキーボード・利用案内 | 成功・ソースと媒体照合済み |
| `20260918T115402Z-mvp-ui-5f4953` | Releaseの本体標準操作 | 成功・ソースと媒体照合済み |

UI runの`review.json`には実際に確認した画像と抽出時刻を記録した。公開URLとブラウザー閲覧欄は未記入であり、外部添付を含むレビュー完了の検査は実行していない。

## データと非同期操作

対象ソースのReleaseテストは`20260918T115834Z-test-15cc28`。Swiftテスト82件、パラメーター展開後94件が成功し、失敗・skipは0件だった。同じdocument内での選択位置変更を拒否するケースを含む。

| 観点 | 検査した契約 |
| --- | --- |
| 保存領域 | 未作成ならDBを作らない。未知のschemaを移行しない。書き込み接続を閉じ、ファイルとディレクトリの書込権限を外しても読み取れる |
| 対象集合 | 保存済み53件の50件・3件のページ、ピン留め、下書きと削除済みの除外 |
| 原文・対象の照合 | 空白・改行・タブ・結合文字・NUL・絵文字を保持。一覧取得後、本文読み取り前の更新・削除は利用を拒否 |
| 読込競合 | 古い読込の成功・失敗が、新しいフィルターのページやエラー状態を置き換えない |
| 挿入の寿命 | 本文取得中の重複、入力先・同一document内の選択位置変更、非表示、更新、キャンセル後の挿入を拒否。キャンセル後に再操作できる |
| コピーの権限 | 本文取得中に権限が取り消された場合、コピーを実行しない |
| 権限 | フルアクセスなしでも挿入できる。コピーは独立した副作用で、許可を取得前後に確認 |

## 共通検査と本体の回帰

`nix flake check --no-update-lock-file --print-build-logs`はApple Silicon macOSで全7検査が成功した。Python回帰121件、共通UI設計ツール35件、Swift規約の対象54ファイルを含む。配布構成のテストはダミーのarchiveを使い、キーボードの欠落、識別子・version・extension point・権限宣言の不一致を拒否する。この自動テストはmockによる構成検査であり、認証・署名・送信の実行結果を示さない。実際のアップロードは配布の記録で扱う。

本体のRelease UI run `20260918T115402Z-mvp-ui-5f4953`は、標準driver `scripts/check-mvp-ui.py`で成功した。作成、下書き再開、保存、検索、編集、原文コピー、ピン留め、削除・同じUUIDの復元、操作位置設定、編集中のバックグラウンド復帰を検査した。

原本画像`finished.png`、`editor.png`、`trash-search.png`を開いて確認した。録画は213.255秒で、11.685秒（復帰中の起動画面）、106.490秒（検索入力）、191.998秒（削除済みの検索入力）の抽出フレームを確認した。操作全体の成否はdriverの照合結果、視覚確認はこの画像・時刻の範囲として区別する。全編再生と外部への添付は未実施。

## ReleaseのOS統合と画面

`20260918T114010Z-keyboard-ui-54ce58`は、製品をビルド・インストールした後に入力先を切り替える監督付き操作runである。専用Simulatorの同時操作を防ぐ排他ロックを取得し、操作JSONと使用した補助driverを保存した。本体起動時はPIDを確認し、その後の入力先と拡張はUI・本文照合で観測する。本体のPID監視を別アプリの生存確認として使っていない。

| 確認 | 結果 |
| --- | --- |
| OSへの追加 | iOS設定でnibbleを追加済み。実際のフルアクセスswitchのオフ・オンを読み取り、許可の確認画面を操作した |
| 権限なしの挿入 | VerificationAppのUITextViewへ項目を挿入。入力先で「すべてを選択→コピー」を行い、保存済み本文66 byteとUTF-8で完全一致 |
| 権限なしのコピー | 別のsentinel文字列をクリップボードへ置き、コピー操作後も同じ値であることを照合。入力先も変わらず、設定案内を表示 |
| 権限ありのコピー | クリップボードが保存済み本文とUTF-8で完全一致。入力先は空のまま |
| 対象と更新 | すべて／ピン留めの切替と更新。対象・操作結果を画像と録画で確認 |
| 表示 | 通常288pt、Safariの横向きでcompactの196pt。上部・下部操作とスクロール領域が収まり、ライト／ダークで内容を識別できる |
| 復帰 | 地球儀の長押しから標準のEnglishキーボードへ復帰。フルアクセスはオフ、外観はライト、端末は縦へ戻した |
| 本体の案内 | 設定から開き、追加・権限・制約の全文をスクロールして読み、標準の戻る操作で復帰 |

テスト本文は改行、先頭・末尾の空白、タブ、結合文字、絵文字を含む。入力先のAX値は空白を省略するため、AXの文字一致と原文byte照合を分けた。キーボードの子要素はsim-use 0.14.0で取得できないか、横向きでは拡張内の相対座標になることがある。取得した原本画像に基づき座標を選び、コピー・挿入は結果を別途照合した。

Safariには既存の`validation/HostForm.html`をlocalhostから配信した。Simulatorのウィンドウ操作はOrca computer-useで対象名・window IDを確認して行い、回転・キーボード表示の操作ログを保存した。検証用アプリの再起動と案内画面のスクロールは補助コマンドとして記録した。これらの操作は製品コードの変更を伴わない。

### 画像と録画

原本PNGで挿入、コピーの操作面、権限案内、配色、横向き、利用案内を確認した。`review.json`には`keyboard-insert-without-permission.png`、`keyboard-copy-ready.png`、`keyboard-copy-off.png`、`keyboard-dark.png`、`safari-keyboard-layout.png`、`keyboard-guide.png`、`keyboard-guide-end.png`と録画を記録した。横向きの根拠はSafariの`safari-keyboard-layout.png`である。

録画`recording.mp4`は10.118秒。抽出時刻0.758、4.292、8.663秒の3枚を開き、コピー結果、挿入結果、フィルター変更を確認した。全編再生、公開先へのアップロード、レビュー担当者の閲覧は未実施。媒体はGit対象外のrunディレクトリに置く。録画には検証コマンドの待機を含むため、長さをアプリの応答時間として使わない。

## 容量と性能の境界

通常ReleaseのSimulator用`NibbleKeyboard.appex`は`du -sk`で1,596 KiB。署名済みのarm64・x86_64用生成物であり、実機のIPAやApp Storeのダウンロード容量ではない。

表示・挿入・コピー・回転後、拡張プロセスのホスト側`ps`によるRSSは179,264 KiB（1回、稼働9分34秒）だった。Simulatorの共有framework等を含むRSSであり、実機のメモリ上限に対する安全性や、ピークのphysical footprintを示さない。実機の拡張メモリ、表示レイテンシー、スクロールのhitch・fpsは未測定であり、速度改善は主張しない。

モデルが保持する要約は1ページ50件。Readerは続きの判定のため最大51件を取得し、本文は利用操作時に1件を読む。接続は要求ごとに閉じる。これは保持件数と所有関係の契約であり、プロセス全体のメモリ上限や応答時間の計測結果ではない。

## 付録：開発時の診断記録

| run・条件 | 観測と対応 |
| --- | --- |
| `20260918T105032Z-build-700b1a` | UIKit bridgeのMainActorと比較conformanceが不一致。型へMainActorを明示 |
| `20260918T105238Z-run-8da504` | Swift 6.3.2のIR生成がBindingのメソッド参照でクラッシュ。setterを明示的クロージャへ変更 |
| `20260918T105509Z-run-583504` | Debugのビルド・インストール・本体起動成功。初期キーボードで権限なし読込・挿入を確認 |
| `20260918T110750Z-test-4ff7a5` | Releaseで93件成功。選択位置変更のケースを含む94件の対象runとは区別する |
| テスト構成の拡張起動 | 動的Package frameworkへの検索パスがなく、起動直後に標準キーボードへ戻った。拡張と本体のFrameworksを含むrunpathを設定 |
| `20260918T112703Z-keyboard-ui-c50c27` | 補助driverのPID監視解除が不完全で、`ps -p None`に失敗。別アプリへ遷移するrunの監視範囲を明示して修正 |
| `20260918T113105Z-keyboard-ui-64f82c` | 権限なしの挿入・コピー拒否が成功。後半のコピー試験はソフトウェアキーボード非表示のまま操作し不一致。失敗runを保持 |
| `20260918T114010Z-keyboard-ui-54ce58` | 表に記載したRelease UI操作が成功。Simulatorのソフトウェアキーボード表示を戻し、画面を観測してから操作した。使用不能なページ矢印を弱い色で表示 |

Simulatorのソフトウェアキーボード表示は[AppleのSimulator解説](https://developer.apple.com/videos/play/wwdc2019/418/)にあるCommand-Kを利用した。回転した端末上でもVerificationApp自体は縦固定のため、横向きの評価にはSafariを使った。`keyboard-landscape.png`はその縦固定を確認した画像であり、横向き成功の証拠には使わない。

## 記録の適用範囲

署名済みIPAは`0e401c19cd75c429dcc135e17aafb37b0e7cf242`、アップロードは`96edc861282173a5378ea4607e61698794f3b2f8`を対象に実行した。公開manifestと結果の対応は[配布の検証記録](testflight-validation.md#キーボードを含む内部配布)に定義する。Simulatorの署名はad hocであり、配布署名の成立とは分けて記録する。

すべての入力先アプリによる文字受理、実機のApp Group読み取り・挿入・コピー、メモリ上限とロック中のData Protectionは未検証。ページ取得は53件の単体テストで確認したが、50件を超える実画面操作は未検証である。コピーの画面離脱・更新・キャンセル時の拒否、および入力先変更時にコピーを継続する条件は実装をレビューした範囲で、専用の遅延テストは実行していない。
