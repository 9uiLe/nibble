# 操作完了通知と削除取り消しの検証記録

## 評価結果

本体の一覧・検索にコピー、削除、復元の結果を短時間表示する機能を評価した。一覧での通知と取り消し、画面離脱時の通知終了は確認できた。検索語を入力してキーボードを表示している間は通知が見えず、表示期限だけが進むため、検索中の要件は未達成である。機能全体の受け入れは未完了とする。

採用する通知内容・状態管理・通常時の配置は[設計仕様](design/decisions/0004-tab-accessory.md)、検索中の配置条件は同文書の[未決定条件と受け入れ](design/decisions/0004-tab-accessory.md#未決定条件と受け入れ)に定義する。関連タスクは[Issue #26](https://github.com/9uiLe/nibble/issues/26)。

## 対象と環境

以下のrunは、ビルド・操作・撮影を一つにまとめた検証実行を指す。生成物は各作業ツリーの`artifacts/ios/<run ID>`に保存する。

- 対象ソース: `ecacb21a8eaca320ef3927d92593f09ae44f9703`。比較元は`5d9d2fe651bfafe8eecb7a1c7b78a337f754acfb`。
- 確認日: 2026-09-19。Xcode 26.5 (17F42)、Swift 6.3.2、iOS 26.5 (23F77)。
- 専用Simulator: iPhone SE (第3世代)、375×667pt、UDID `A566331E-BF10-48B5-9A79-74D7177430EF`。
- Release / `-Osize`、テストと診断用UIのbuildは`ENABLE_TESTABILITY=YES`。通常の配布archiveとの同一性を示す検証ではない。
- UI操作はNixで固定したsim-useを使い、App Groupの既存DBをバックアップしてダミーデータへ置き換え、run終了時に復元する。

## 自動テストと共通検査

`ios.py test --project-config app/project.json --configuration Release --device <上記UDID>`のrun `20260919T083131Z-test-77c281`で115件・13 suiteが成功した。対象ソースとの`check_evidence.py --integrity-only`照合も成功した。

状態管理のテストでは、表示開始の通知を受ける前に読み上げ・触覚・期限を発生させないことと、同じ通知IDの再表示で期限と効果を再利用することを確認した。新しいID、旧期限との競合、キャンセル後の再開、タブ・シート・非アクティブ化、遅れて完了するコピー・削除、現在のUUIDだけの復元、復元の重複防止と失敗表示も対象とした。表示開始の入力を与えるテストであり、実画面で通知が見えることは別に評価する。

UIKit接続のマウントテストは、26.5上でaccessoryの追加・除去、選択中の子コントローラーの同一性、非表示時のsafe areaの復帰を確認した。26.0 runtimeでの評価には代えない。

`nix flake check --no-update-lock-file --print-build-logs`は7項目すべて成功した。コードと規約の照合では、タイマーの非キャンセル例外が診断ログへ記録されることと、通知の所有・表示開始・期限の契約が実装と一致することを確認した。

## 一覧の画面操作

run `20260919T083647Z-accessory-ui-6b1757`は12件のダミーデータで成功し、対象ソースとの証跡照合も成功した。コピー→消去、削除→取り消し→消去、ダークで長い対象名を持つ削除、削除直後のコピーを操作した。取り消したUUIDがDBで`deleted = 0`になったことを照合した。

- コピー通知は対象名・取り消しを持たず、削除通知は対象名と44pt高のボタンを持つ。削除直後にコピーすると復元対象とボタンが同時に消える。
- 意味情報は省略前の対象名と結果を持ち、取り消しは独立したButtonとして現れる。実際のVoiceOver操作は未実施。
- コピー前・表示中・消去後の先頭行はy=186ptを保つ。新規作成ボタンはy=520→464→520ptで、非表示時に元の下部領域へ戻る。
- ライト／ダークの画像とコピー・削除・復元・消去の画像を開いて確認した。動画は15.658秒の原本から1.202、8.047、13.378秒の3フレームを確認した。全編再生、アップロード、ブラウザー閲覧は未実施。
- 変更前の同じ12件の画像は比較元のrun `20260919T083305Z-accessory-ui-3c31b8`で取得し、比較元ソースとの照合と画像の目視を完了した。

## 画面遷移とスクロール

run `20260919T084308Z-accessory-ui-772779`では、削除通知から別タブへ移動して戻る、編集シートを開閉する、ホームへ移動して同じプロセスへ戻る操作を確認した。各復帰でアプリ識別子`nibble.9uiLe.com`と「一覧」の見出しを読み取り、終了した通知と取り消しボタンが再表示されないことを確認した。

リスト末尾まで移動して最後の項目をコピーでき、コピー後も末尾付近の表示を保った。使用回数による並び替えでコピー対象自体は先頭へ移動するため、同一項目の位置が変わらないことを示す結果ではない。対象ソースとの照合は成功し、末尾の画像も目視した。

## 検索中の未解決事項

検索タブで検索語「0」を入力し、キーボードを開いたまま結果をコピーする手順では、通知が画面・意味情報の両方に現れなかった。6秒間の削除通知でも同様だった。通常の一覧タブでは表示される。

再現runは`20260919T081610Z-accessory-ui-889068`と`20260919T082401Z-accessory-ui-35daa4`。表示直後の画像、意味情報、操作ログを保存した。診断用ログでは検索タブの世代が有効で、結果が発行され、表示タスクが開始していることを確認した。診断用ログは製品コードから除去した。

| 切り分け | run | 観測 |
| --- | --- | --- |
| 検索タブの状態・発行・表示タスク | `20260919T082623Z-accessory-ui-c2ef48`とlog stream | 検索の世代は有効。結果発行と表示タスク開始を確認 |
| タブバーのvisible指定 | `20260919T082708Z-accessory-ui-bf2ef6` | 検索中の通知は表示されない |
| 公開UIKitのbottomAccessory | `20260919T082923Z-accessory-ui-b2efa4` | 検索中の通知は表示されない |
| 検索欄のnavigationBarDrawer指定 | `20260919T083016Z-accessory-ui-5857e0` | 検索中の通知は表示されない |

UIKit接続をNavigationStackの外側へ配置した診断run `20260919T082807Z-accessory-ui-96a0a2`は、検索の自動入力開始が成立せず中断した。NavigationStack内へ配置した構成では標準検索を操作できたが、通知は表示されなかった。採用構成は内側への配置であり、この診断結果はiOS 26.0 runtimeでの動作を示さない。

システムが通知を隠している間に期限だけが進む動作は、「画面に通知を出した時点から計測する」「キーボード表示中も結果を読み取れる」という受け入れ条件を満たさない。検索入力中の配置を決定し、読み取り・取り消し・表示期限を実画面で確認する必要がある。

## 判定から除外した実行

`20260919T084005Z-accessory-ui-fff7b5`では、ホームへ移動した直後に`simctl launch`で復帰を試みたが、ホーム画面のままだった。通知がないという条件だけでは別アプリの画面も通過したため、このrunのバックグラウンド復帰は確認済みとしない。`20260919T084155Z-accessory-ui-d408d2`ではホームへのアニメーション中に画面を読み取り、中断した。

バックグラウンド復帰の合格判定には、ホームへの遷移完了とアプリ識別子の照合、観測したnibbleアイコンからの復帰、nibbleが前面にあることの確認を必要とする。上記の成功run `20260919T084308Z-accessory-ui-772779`はこの手順で実施した。

## 通知とスクロールの負荷

同じSimulator・Release / `ENABLE_TESTABILITY=YES`・1万件のダミーデータで、5回のコピー対象タップ、上下一組のスワイプを5回、通知の消去待ちを1試行とし、3回測定した。hostの`ps`が返すnibbleプロセスの累積CPU時間の差を使う。分解能は0.01秒で、描画以外のアプリ処理も含む。wall時間はdriverの待機やUI読取を含み、操作遅延やフレーム時間を示さない。

| 対象 | CPU秒（3試行） | 中央値 | wall秒（3試行） |
| --- | --- | --- | --- |
| 比較元 `5d9d2fe` | 2.03 / 1.93 / 1.92 | 1.93 | 22.304 / 22.837 / 22.816 |
| 実装 `ecacb21` | 2.30 / 2.06 / 1.95 | 2.06 | 22.840 / 22.629 / 22.312 |

中央値は+0.13 CPU秒（約6.7%）。3試行の限定的な比較であり、高速化、実機の負荷、FPSやhitchの許容値達成を主張しない。runは比較元`20260919T083417Z-notice-cpu-686e78`、実装`20260919T083807Z-notice-cpu-2ec0f9`で、両方の対象ソースとの照合が成功した。

## 未実施項目

iOS 26.0 runtime、実機、VoiceOverの実読み上げ・実操作、触覚の実機確認、FPS・GPU・hitchの計測は未実施。意味情報のラベルとボタン、記録用effectsでの呼び出し回数は実際の読み上げ・触覚と区別する。
