# UI部品台帳

C番号は操作・表示の責務を識別する。構成と理由、制約、評価条件を部品単位で定める。画面の導線は[S台帳](screens.md)、共通表示は[F原則](foundations.md)、R番号は[根拠](evidence.md)を参照する。各条件は受入基準であり、実行済みの申告ではない。

## ナビゲーションと一覧

項目名は`SnippetTextPresentation`で整形し、保存済み・下書き・Keyboardの行は`SnippetHeading`を使う。本体の2行・左側ピンとKeyboardの1行・右側ピンは表示文脈の差として保持する。`PinButton`は同じ状態と操作名を受け取り、メニュー／スワイプのラベルとKeyboardのアイコンを描き分ける。絞り込み・操作資格・更新処理は呼出元のモデルと保存層が担う。

実装：[AppRootView](../../app/Nibble/Navigation/AppRootView.swift)、[LibraryScreen](../../app/Nibble/Library/LibraryScreen.swift)、[SnippetRow](../../app/Nibble/Library/SnippetRow.swift)、[SnippetRowContent](../../app/Nibble/Library/SnippetRowContent.swift)、[LibraryFilterBar](../../app/Nibble/Library/LibraryFilterBar.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C01 作業画面と設定 | 一覧と検索を一つのNavigationStack内に置き、右上の設定から階層移動する。モデルはルートで保持する | 探し方の変更だけで別画面へ移動せず、同じ集合・検索語・操作対象を使う | S01・S02、F06。設定・編集から戻ったときに集合と検索語を保ち、検索語消去で検索前の集合へ戻る |
| C02 画面タイトル | RootScreenHeadingは左にnibble、右に設定入口を置く。文字はScreenHeadingへ渡す。階層と編集シートは標準inlineタイトルを使う | 保存総数はフィルター、検索対象は入力欄へ集約し、重複する補足行を作らない | R03・R05、F03。狭幅でもタイトルと設定が重ならない |
| C03 背景と区切り | 共通背景のListに文字を並べ、区切り線で項目を分ける | 連続した文字を読み比べやすくする。カードは独立性を示せるが境界と余白が増える | R04・R15、F01・F02。行・見出し周囲・余白を統一し、文字と境界を識別できる |
| C04 フィルター | すべて・ピン留め・下書きから、初回または更新時に保持した集合を即座に選ぶ。選択状態と総数を示す | 切替そのものでは読込を開始しない。外部変更はListのPull to Refreshで3集合を更新する。保存やピンなど自身の操作結果は自動反映する | R03・R06。未取得・失敗を0件と混同せず、100件を超える集合の切替・追加取得・更新で項目と件数を維持する |
| C05 区分見出し | 直近1件の下書き再開帯と保存済み一覧を区分する。残りの下書きは「ほかN件」から専用フィルターへ案内する。保存済み・ピン留め・検索結果には集合名と「コピー回数順」、削除一覧には重複した見出しを置かない | 未完了の再開と保存済みの利用を区別する。保存済みをピンで分割すると使用回数の全体順を失うため、単一集合にする | R03・R04。空区分を省き、多数の下書きが利用対象への到達を妨げないか評価する |
| C06 保存済み行 | 項目名と要約を示し、内容部分のタップで編集する。無題は本文から表示名を作る。本体の最終コピーから720時間以上の項目には「30日以上コピーしていません」と最後のコピー日を補足する | 名前から要約の順で対象を識別する。要約だけは空白・改行を折り畳んで2行以内に表示し、原文を変更しない。内容部分を編集、右端をコピーへ割り当て、対象確認と即時利用を使い分ける | R04・R14。同名・無題・長文を区別でき、全文へ到達できる |
| C07 下書き行 | 通常一覧は「下書きを再開」・最新1件の表示名を帯で示す。下書き専用一覧ではF03のタイトル・本文要約・更新日時を左揃えにし、末尾の矢印で再開を示す。無題は本文先頭を名前にする。行全体のタップで再開し、コピーは置かない。見出しで再開操作、末尾で保存後の利用を案内する | 下書きの区分と再開の文言で未完了を伝え、左の装飾記号を省いてタイトル・要約の幅を確保する | R04・R13。複数の下書きを識別し、選んだ編集を再開できる |
| C08 追加取得 | List末尾の「さらに表示」で続きの保存済み項目または下書きを取得する | 続きの存在と読込の開始を明示する。無限スクロールより終端と取得の制御を優先する | R12、F07。100件単位で取得し、表示済みの対象とフォーカスを保つ |
| C09 新規作成 | 作業画面の下部に文言付きの全幅ボタンを固定する。終了時は選択中の集合・検索語へ戻る | 作成と対象のコピーを位置と面積で区別する。検索入力中は隠し、入力を優先する。設定は管理と案内に専念し、作成入口を重複させない | S01〜S04、F05・F06。下部ボタンは高さ48pt以上で、最終行や通知と重ならない |
| C10 コピー | 対象行の右側に淡い角丸の背景と44ptの領域を持つコピーボタンを置く。本文をコピーする。OSへの書込呼出しが戻った時点を使用として記録し、順位を再取得する | 対象を開かずに利用できる。詳細画面への移動が必要な構成より操作を減らす | R07・R11、F05。行の編集と誤って同時実行せず、対象名と操作を読み上げる |
| C11 整理操作 | 行の可視「その他」メニューから編集・ピン留め・削除を選ぶ。長押しにも接続する。保存済みの行は左スワイプで削除、右スワイプでピン留め／解除を示し、フルスワイプでも実行する | 対象に従属する整理を一つの可視入口へまとめ、削除を最後に置く。可視入口で発見性を補い、左右一つずつの操作で繰り返しの選択を減らす。削除は取り消し可能な削除済み状態への移動とし、可視操作は文字設定にかかわらず内容の横に置く | R08。ジェスチャーの代替、ピン状態の識別、誤削除からの回復を評価する |

## 検索と状態表示

実装：[AppRootView](../../app/Nibble/Navigation/AppRootView.swift)、[LibraryScreen](../../app/Nibble/Library/LibraryScreen.swift)、[LibraryModel](../../app/Shared/Application/LibraryModel.swift)、[LibraryNotice](../../app/Nibble/Library/LibraryNotice.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C12 検索入力 | 作業画面の見出し下に「タイトルや本文を検索」を固定する。入力欄をタップして開始し、完了後も語句を保つ。キーボードを閉じる操作は検索欄内の固定幅領域に出す | 検索専用画面を経由せず、対象の探索から利用までを一か所で行う。入力終了操作の出し入れでも欄と入力文字の幅を変えず、検索語がある間は集合選択を隠す | S02、F05。フォーカス前後で欄と空の入力フィールドの位置・幅を保ち、日本語変換と検索確定を区別できる |
| C13 未検索の表示 | 検索語が空白だけのときは選択中の集合を表示する。検索語を消すと検索前の集合と取得上限へ戻る | 未検索専用の説明や重複する虫眼鏡を省き、保存済みの識別に面積を使う | S01・S02。0件・失敗・未取得を区別し、検索結果が保持中の集合を置き換えない |
| C14 検索0件 | 語句に一致する項目がないことと修正方法を伝える | 保存データ全体が空である状態から区別する。自動の作成誘導は重複作成を促す可能性がある | R13。検索語を保持し、修正・消去・終了で回復できる |
| C15 空状態 | 一覧の集合ごとに空の意味と次の操作を示し、作成・対象変更には文言を持つ標準ボタンを使う。見出し・説明・操作名はF03に従い、記号と操作領域の大きさを保つ | 初回作成、対象変更、下書き再開、復元は異なる文脈を持つ | R13、F07。読込中と失敗を空状態と取り違えず、作成・対象変更・閉じるへ進める |
| C16 読込表示 | List内で初期・更新・追加取得の進行を伝える。検索・削除一覧の検索語変更中は表示中のsnapshotを操作不可で保持し、先頭に読込中の要求を示す。本体一覧の保持済みフィルター切替は読込表示を出さない。中断時は進行表示を終え、44 pt以上の「読み込みを再開」を先頭に置く | 内容の文脈を保つ。全面の進行表示は更新ごとに内容を隠す負担がある。中断をエラーや0件と扱わず、同じ要求を再実行できる | R12。選択直後から完了までの中間状態を検査し、古い結果・取消・失敗で空表示を確定しない。測定できない進捗割合を示さない |
| C17 失敗と再試行 | 失敗した処理・理由・回復方法を空状態より先に示す。読込の再取得、コピーの失敗、コピー済みの使用記録失敗を分け、回復操作は枠付き・最小44 ptの独立したボタンにする | 探索や入力を保持したまま回復する。短時間通知だけでは必要な行動を読み逃しやすい | R10・R13。失敗した対象が分かり、「一覧を再読み込み」は再取得のみを行い、コピー・削除の再実行と誤認させない。「回数と日時を記録し直す」は本文をコピーせず、同じ操作IDの記録だけを再試行する |
| C18 完了通知 | 一覧・検索・削除一覧の専用UIWindowで作成操作またはキーボードの上に左右16ptの全幅カードと読み上げ・触覚で完了を伝える。表示資格中はhostを維持し、ScopedAnimationで0.3秒の不透明度と8ptの移動を補間する。不透明なニュートラル色の背景と反転した文字色を使い、結果文を太字で優先する。削除・復元の対象名は1行で省略する。表示資格・入力透過・削除シートの扱いは[通知設計](rationale/result-notices.md)に従う | 一覧を維持して入力先への復帰を支え、背後の文字や面が透けることによる読みにくさを防ぐ。毎回の確認ダイアログは操作を中断する | R11、F02・F07・F08。起動直後のコピーを含め、成功後に2秒（削除は6秒）表示し、連続操作・期限交差・画面終了で古い状態を残さない。[通知の設計](rationale/result-notices.md)に従う |
| C19 削除取り消し | 対象名付き削除通知と見た目36pt・操作領域44ptの「元に戻す」アイコンを同じ横並びのまとまりに置く。ボタンは通知背景と反転する円形で識別し、処理中は操作を無効にする | 直前の誤操作から短い手順で回復する。期限後の復元は削除一覧が担当する | R10・R11。通知IDと対象UUIDが一致し、連打を重複実行せず、失敗には再試行を示す。連続削除と通知消去後も復元できる |

## 編集

入力中・非入力時の補足入口は共通の`EditorHelpButton`を使う。配置と排他的な表示、フォーカス復帰は各コンテナに残す。補足と保存失敗に示す上限はDomainの定義から生成する。

実装：[SnippetEditor](../../app/Shared/Editing/SnippetEditor.swift)、[EditorModel](../../app/Shared/Application/EditorModel.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C20 編集シート | 一つの作成・編集に集中し、終了時に呼出元へ戻る | 一時的な作業と利用の一覧を分ける。シート内で入力と終了条件を統一する | R09。新規・保存済みの編集・再開・共有で入力を保持する |
| C21 タイトル入力 | 必須の本文の後に任意の識別名を入力する。常設ラベルで任意と伝え、プレースホルダーは入力例にする | まず利用する本文を作り、必要な人だけ一覧の目印を付ける。必須にすると短い定型文にも命名の負担が加わる | R13・R14。空でも本文を保存でき、長い名前は折り返す |
| C22 本文入力 | 最初の内容領域でMarkdown原文を編集し、「入力」と「プレビュー」を切り替える。変数追加は別の選択面で既存名の再利用と新規作成を明示し、印を本文末尾へ加える。空の新規作成では本文にフォーカスする | 本文の作成を先に置き、差し替えの説明は入力面の後へ置く。既存名の再利用では同じ値の入力欄を増やさない。装飾と差し替えは原文を変えない | R14、F09。日本語変換、選択、取り消し、既存変数の複数回利用、空白・改行・Unicode・Markdown記号の保持を確認する |
| C23 ペースト | 本文の補助操作行に「末尾にペースト」と標準PasteButtonを置き、変数追加と同じ段へ整理する。利用条件はC31の補足に示す | クリップボードの自動読取をせず、OSの明示操作を使う。主入力より強調しない。追記はEditorModelを経由し、他の入力と同じ変更・保存契約に従う | R13、F05・F09。テキストの有無、末尾への追記、本文の原文保持を確認する |
| C24 保存 | NavigationBarのconfirmationActionに文字の「保存」を置き、保存済み項目への反映を確定する | 操作の意味を記号の解釈に依存させない。保存不可は標準の無効表示に任せる | R05・R09。本文が空白のみなら無効。成功時だけ閉じ、重複保存しない |
| C25 閉じる | NavigationBarのcancellationActionに文字の「閉じる」を置き、下書きを保持して戻る | 中断と確定を分け、直下の短い案内で下書きに残ることを伝える | R09・R13。最新入力の保持成功を待ち、失敗時は画面に残す |
| C26 その他メニュー | 下部バーの「…」アイコンのみの入口に本文共有と下書き破棄をまとめる。操作領域は44pt以上、読み上げ名は「その他」を保つ | 低頻度の操作を可視の入口へ収め、入力面積を確保する。確定・中断とは操作の階層を分ける | R05・R08。必要な操作を発見でき、保存済み項目の削除と下書き破棄を区別できる |
| C27 本文共有 | ShareLinkで編集中の本文をOSの共有先選択へ渡す | 標準の共有先と操作を使い、独自の共有先管理を持たない | R08、F09。本文が空なら無効。共有から入力を保持して戻れる |
| C28 下書き破棄 | 確認ダイアログで下書きの内容が元に戻らないことを伝え、除去を確定する | 保存済みを残すことを明示し、中断との意味を分ける | R10・R13。取消で入力を保ち、確定時だけ下書きを除く |
| C29 終了処理 | 入力を固定し、「保存中」「下書きを保存中」「下書きを破棄中」で進行を示し、一度だけ完了させる | 処理中の入力変更による確定対象の曖昧さを防ぐ | R12、F07。状態の説明が実行中の操作に合い、失敗時に操作可能へ戻る |
| C30 編集の失敗 | 入力の近くに理由を残し、対応可能な競合には別項目として保存する入口を示す | 入力と回復の関係を保つ。自動上書きは別の編集を失わせる可能性がある | R10・R13。失敗した保存・閉じる・破棄を区別し、画面に残る入力と再操作を案内する。別項目への保存が失敗した後も再試行の入口を残す。利用者が選んだ場合だけ別項目を作る |
| C31 終了の補足 | 保存・閉じるの短い結果説明を上部の操作直下に常設する。「入力と保存について」は非入力時に画面下部、入力中にキーボード上へ一つだけ置く | 終了操作の近くで意味を理解できるようにする。補足シートには原文保持、ペースト条件、UTF-8上限、下書きが残らない例外をまとめる | R13、F06・F09。入力中も短い説明が見え、補足の往復で入力先と内容を復元する |
| C32 キーボード操作 | キーボード上に文章の補足入口とキーボードを閉じるアイコンを置く。safe areaの同じ行に配置し、キーボードとの間を8pt空ける | 操作行には不透明な背景を持たせ、スクロールする本文と重ねて読ませない。背景だけをsafe area外まで広げ、入力欄と操作はキーボードを避ける | R19、F04。日本語変換、長文の末尾、補足からの復帰、狭幅で、本文・操作・キーボードが重ならない |

## 設定・復旧・製品情報

実装：[SettingsView](../../app/Nibble/Settings/SettingsView.swift)、[AboutView](../../app/Nibble/Settings/AboutView.swift)、[DeletedSnippetsView](../../app/Nibble/Library/DeletedSnippetsView.swift)、[LibraryScreen](../../app/Nibble/Library/LibraryScreen.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C51 Proの入口と案内 | 設定の先頭から開く。現行の無料機能とProの準備状態を説明し、登録中なら管理入口を示す。販売を有効にするときはStoreKitの商品価格と復元操作を示す | 公開準備中に購入を促さず、既存の登録者は状態を確認できる | S04、F05・F09。価格が商品情報と一致し、商品未取得・購入取消・復元・期限切れ後も既存項目を利用できる |
| C34 削除一覧の入口 | 設定の「削除した項目」に「復元・完全削除」を添え、末尾に「›」を付けて削除一覧へ進む | キーボード案内の次に管理の入口を置き、削除済みの対象と復元場所を直接示す | R06、F09、G11。復元場所を初見で発見できるか評価する |
| C35 製品情報の入口 | 「操作とデータについて」を添えたNavigationLinkで「nibbleについて」へ進む | 説明を設定の下位階層として扱い、日常の操作と管理の入口より後に置く | R05。内容を予想でき、標準戻るで設定へ戻れる |
| C50 バージョン情報 | 設定の下部にCFBundleShortVersionStringとCFBundleVersionを「バージョン release (build)」で表示する。遷移や「›」、バージョン表示の区切り線は付けない | 操作項目ではなく補足情報として示す。利用中の版を設定から確認できる。ビルド時に付与された値を読み、配布版を固定文字列で誤表示しない | S04、F09、R06。インストールしたbundleと表示が一致し、狭幅でも読める |
| C36 製品情報 | 導入、基本操作、共有、ショートカット、データの扱いを一列の文章で説明する。見出しは本文と一緒にスクロールし、URLは用途ラベル付きの選択できる文字で示す | 行の区切りや固定見出しを使わず、余白とF03の共通文字階層で読み順を示す。URLも同じ本文書体に揃え、独立表示で日本語の段落幅を保つ | R03・R13・R14、S05。小画面・固定した標準文字で長文を省略せず読み切れ、端末内保存と削除の意味を理解できる |
| C37 削除済み行 | 名前と要約で対象を識別し、対象名を読み上げる復元と可視メニューを置く。内容は編集ボタンにしない | コピー・編集の対象から復元対象へ操作を切り替える | R04・R11。対象名が分かり、同じUUIDと原文で復元できる |
| C38 完全削除 | 対象名、回復不能であること、関連下書きの除去を確認する | 直接実行より対象確認と誤操作の防止を優先する | R10。対象と結果を理解し、取消で維持できる |
| C39 削除一覧の検索・終了 | 削除済みだけを検索し、閉じるで設定へ戻る | 復旧と通常検索の対象範囲を分ける | R02・R09。0件・失敗・復元後に回復でき、新規作成を表示しない |

## システムと共有拡張

実装：[AppRootView](../../app/Nibble/Navigation/AppRootView.swift)、[SnippetEditor](../../app/Shared/Editing/SnippetEditor.swift)、[ShareViewController](../../app/NibbleShare/ShareViewController.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C40 非アクティブ表示 | アプリ切替時に本文を内容のない面と製品名で覆う | 切替画面への本文の露出を抑えるため、内容より前面へ置く | 製品の保護方針。シート、OSの画面取得時点、共有拡張での適用条件を評価する |
| C41 共有取込 | テキストまたはURLを1件受け取り、下書きを作って共通編集へ渡す。「共有から保存」の見出しと確認の案内を入力前に置き、「閉じる」で共有元へ戻ることを補足する | 内容を確認してから保存し、本体と同じ編集契約を使う。取得していない共有元名やページ情報は表示しない | C20〜C32。遅延・取消・取得失敗で状態と終了方法を理解できる |
| C42 取込失敗 | 編集開始前の問題を標準alertで示し、閉じるで共有元へ戻る | 入力可能な状態と取得失敗を区別する | R10・R13。非対応形式、空白のみ、長さ超過、取得・保存先の問題を分ける。共有元での修正・再共有を案内し、編集開始前に「保存」を押すよう指示しない |
| C43 OS所有の要素 | ステータスバー、ホームインジケータ、キーボード、検索の標準操作、シート外形、共有先選択 | OSの学習済みの操作と適応を使い、独自描画の保守を減らす | R03・R09・R19。セーフエリアを守り、必要なOS操作を妨げない |

## 説明の動き

実装：[AboutIllustration](../../app/Nibble/Presentation/AboutIllustration.swift)、[KeyboardIllustration](../../app/Nibble/Presentation/KeyboardIllustration.swift)、[RiveCanvas](../../app/Packages/RivePresentation/Sources/RivePresentation/RiveCanvas.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C44 利用の説明イラスト | メモ画面の文章を選び、複製をnibbleへ保存する図と説明。本文幅で自動ループを表示する。周期と再生方針は演出設計に従う | 導入文の後に置き、元の文章・複製・保存先の関係を伝える。自動ループは途中からの閲覧に対応し、再生操作を不要にする。隣接するTextが説明と読み上げを担う | [演出設計](../architecture/presentation.md)、S05・F08、R20・G16。複数周期、再生ボタンなし、不可視時の停止、復帰、配色、読込失敗時の説明と再試行を確認する。理解度と読書への影響は利用者評価の対象 |

## スニペットキーボード

実装：[KeyboardView](../../app/NibbleKeyboard/KeyboardView.swift)、[KeyboardViewController](../../app/NibbleKeyboard/KeyboardViewController.swift)、[KeyboardGuideView](../../app/Nibble/Settings/KeyboardGuideView.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C45 キーボードの対象選択 | 上部にコンパクトな「すべて／ピン留め」と更新を配置し、下線で選択を示す。各タップ領域は44×44pt以上 | 排他的な集合をまとめ、一覧に使う高さを確保する。対象選択と更新を同じ段に配置する | S09、技術仕様0005。選択状態、狭幅、集合切替後の先頭ページと読込状態を確認 |
| C46 キーボードの項目と全文 | 平坦な単一リストと区切り線、タイトルと1行プレビューを置く。行の主領域には「入力」、別の操作には「全文」と文字で示す。全文に戻る・ピン・コピー・入力を置く。変数を含む本文だけ確認へ進み、利用不可なら理由を示して送らない | 操作の結果を短い文字で示し、一覧の密度を保つ。詳細と差し替えは入力先との位置関係を保つためキーボード内に置く。確認中に入力先が変わったら挿入しない | S09。1タップ1挿入、「全文」で0挿入、差し替えの取消・確認、長文末尾、戻る位置、ピンと空状態、原文・権限・連打を確認 |
| C47 キーボードの結果と下部操作 | 短い成功結果と挿入行背景を2秒で消す。失敗・権限案内は一覧または全文のスクロール領域の先頭に表示し、行と本文は残す。案内には期限を設けない。下部に必要なページ操作、閉じる、OS要求時の地球儀を固定する | 長文で下部操作の領域を増やさず、失敗時も対象と回復先を読んで次の項目を選べるようにする。平常時の装飾的な製品名は置かない | S09。操作完了後の表示、通知IDごとの期限、ページ境界、地球儀、狭幅、権限変更を確認 |
| C48 キーボード利用案内 | 設定の「nibbleキーボード」に「追加・使い方・フルアクセス」を添える。案内は用途と追加手順、使用中の操作、許可と利用条件の順にまとめる | 最初に使い始める手順を示し、必要になったときに権限と制約へ進める。長い説明を読む領域は本体に確保する | S10。全段落の可読性、標準の戻る、コピーとピン更新にだけフルアクセスが必要なこと、入力先の反映確認、実際のiOS設定名の理解 |
| C49 キーボード入力の説明イラスト | 入力欄とキーボードを上下に置き、切替・行の選択・本文挿入を自動ループで示す。図は操作を受けず、隣接Textが原文保持を含む意味と読み上げを担う | 導入文と追加手順の間に置き、設定操作の前に用途を示す。構図・比率・周期・結果保持の理由は[演出設計](../architecture/presentation.md#表現と配置の理由)に定める | S10、F03・F08・F10、R18・R19・R20、G15・G16。小画面、ライト/ダーク、複数周期、不可視・離脱・非active時の停止と復帰、配色変更、固定演出、失敗時の説明と再読み込みを確認する |

Keyboardの権限と操作契約は[設計](../architecture/keyboard.md)に従う。表示値は実装を正本とし、上記の理由と評価条件に照合する。
