# UI部品台帳

C番号は操作・表示の責務を識別する。構成と理由、制約、評価条件を部品単位で定める。画面の導線は[S台帳](screens.md)、共通表示は[F原則](foundations.md)、R番号は[根拠](evidence.md)を参照する。各条件は受入基準であり、実行済みの申告ではない。

## ナビゲーションと一覧

実装：[AppRootView](../../app/Nibble/Navigation/AppRootView.swift)、[LibraryScreen](../../app/Nibble/Library/LibraryScreen.swift)、[SnippetRow](../../app/Nibble/Library/SnippetRow.swift)、[SnippetRowContent](../../app/Nibble/Library/SnippetRowContent.swift)、[LibraryFilterBar](../../app/Nibble/Library/LibraryFilterBar.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C01 主要タブ | 一覧・検索・設定をアイコンのみの同じカプセル形バーに置く。通常24pt・縮小16ptの記号を使い、三つの入口を残してバー全体を縮める。下スクロールで縮小、上スクロール・画面切替で復元する | R21の利用者提示画像と修正指定に合わせる。固定レイアウトに連続した拡縮を適用し、素材・選択背景・操作領域を一緒に変形する。内容の表示領域を動かさず、スクロールの観測はバー内で受ける | R01・R21、F05・F06。縮小時も最小44ptを保つ。ライト／ダーク、狭幅、縮小途中・復帰途中・方向反転を確認する |
| C02 画面タイトル | 一覧・検索・設定に共通のLibraryHeadingを使い、文字はScreenHeadingへ渡す。titleと任意のsubTitleを受け取り、F03の画面タイトルを左揃えで固定する。設定は補足なし、一覧は保存済み総数、検索は対象案内を添える。階層内と編集シートは標準inlineタイトルにする | 文字と操作の配置を分け、親の幅と内容の自然な大きさから整列する。補足の有無は表示入力で表し、外側の余白は共通コンテナに集約する | R03・R05、F03。狭幅でも識別でき、補足なしの空行や見出しの重複読み上げがない |
| C03 背景と区切り | 共通背景のListに文字を並べ、区切り線で項目を分ける | 連続した文字を読み比べやすくする。カードは独立性を示せるが境界と余白が増える | R04・R15、F01・F02。行・見出し周囲・余白を統一し、文字と境界を識別できる |
| C04 フィルター | すべて・ピン留め・下書きから一つの集合を選ぶ。選択項目だけを濃淡反転した角丸の塗りで示し、読み上げの選択状態を付ける。集合の総数を添え、幅を保って選択位置へスクロールする | タイトル直下に固定し、表示対象を見失わないようにする。横スクロールは文字幅を保てるが、端の項目と選択状態の発見性が必要 | R03・R06・R16。全選択肢へ到達でき、見た目と選択状態の読み上げが一致する |
| C05 区分見出し | 直近1件の下書き再開帯と保存済み一覧を区分する。残りの下書きは「ほかN件」から専用フィルターへ案内する。保存済み・ピン留め・検索結果には集合名と「使用回数順」、削除一覧には重複した見出しを置かない | 未完了の再開と保存済みの利用を区別する。保存済みをピンで分割すると使用回数の全体順を失うため、単一集合にする | R03・R04。空区分を省き、多数の下書きが利用対象への到達を妨げないか評価する |
| C06 保存済み行 | 項目名と要約を示し、内容部分のタップで編集する。無題は本文から表示名を作る。本体の最終コピーから720時間以上の項目には「30日以上コピーしていません」と最後のコピー日を補足する | 名前から要約の順で対象を識別する。要約だけは空白・改行を折り畳んで2行以内に表示し、原文を変更しない。行全体をコピーに使う案は編集への入口が別途必要 | R04・R14。同名・無題・長文を区別でき、全文へ到達できる |
| C07 下書き行 | 通常一覧は編集記号・「下書きを再開」・最新1件の表示名を帯で示す。下書き専用一覧では淡い背景の編集記号、F03のタイトル・本文要約・更新日時、末尾の矢印で識別する。無題は本文先頭を名前にする。行全体のタップで再開し、コピーは置かない。見出しで再開操作、末尾で保存後の利用を案内する | 保存済みと未完了の内容を区別し、複数の下書きから再開対象を選びやすくする | R04・R13。複数の下書きを識別し、選んだ編集を再開できる |
| C08 追加取得 | List末尾の「さらに表示」で続きの保存済み項目または下書きを取得する | 続きの存在と読込の開始を明示する。無限スクロールより終端と取得の制御を優先する | R12、F07。100件単位で取得し、表示済みの対象とフォーカスを保つ |
| C09 新規作成 | 一覧・検索・設定のルート見出しの右上に＋を置き、編集シートを開く。現在の領域とフィルター・検索語を保持し、終了時は呼出元へ戻る。空一覧の文言付き作成入口も保つ | 利用者の修正指定により、画面を移動する三つのタブと作成操作を分ける。検索入力中は隠して入力を優先する | R07・R18・R21、F05・F06。44ptの操作領域を保ち、右上へ到達できる。検索・設定からの終了時も呼出元を保つ |
| C10 コピー | 対象行の右側に淡い角丸の背景と44ptの領域を持つコピーボタンを置く。本文をコピーする。OSへの書込呼出しが戻った時点を使用として記録し、順位を再取得する | 対象を開かずに利用できる。詳細画面への移動が必要な構成より操作を減らす | R07・R11、F05。行の編集と誤って同時実行せず、対象名と操作を読み上げる |
| C11 整理操作 | 行の可視「その他」メニューから編集・ピン留め・削除を選ぶ。長押しにも接続する。保存済みの行は左スワイプで削除、右スワイプでピン留め／解除を示し、フルスワイプでも実行する | 対象に従属する整理を一つの可視入口へまとめ、削除を最後に置く。可視入口で発見性を補い、左右一つずつの操作で繰り返しの選択を減らす。削除は取り消し可能な削除済み状態への移動とし、可視操作は文字設定にかかわらず内容の横に置く | R08・R16。ジェスチャーの代替、ピン状態の識別、誤削除からの回復を評価する |

## 検索と状態表示

実装：[AppRootView](../../app/Nibble/Navigation/AppRootView.swift)、[LibraryScreen](../../app/Nibble/Library/LibraryScreen.swift)、[LibraryModel](../../app/Nibble/Library/LibraryModel.swift)、[LibraryNotice](../../app/Nibble/Library/LibraryNotice.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C12 上部検索欄 | 検索画面の見出しの下へ固定した標準TextFieldを置く。高さ48ptの欄全体から入力を開始できる。検索語の消去と「完了」で入力を終了でき、欄と語句は保持する。キーボード表示中も結果の上に置く | 利用者の指定により検索対象と結果を同じ上部領域から読み進める。下部ナビゲーションとは役割を分ける | R02・R19、F05。空・入力中・確定後に同じ上部位置を保ち、日本語変換と検索確定を区別できる |
| C13 検索前案内 | 語句が空白のみのとき、検索対象と入力方法を示す。見出しと説明にはF03の文字役割を指定する | 未検索と0件を区別する。履歴や候補の表示にはデータ保持と探索の設計が必要 | R02・R13。入力開始を妨げず、失敗や必要な進行表示を隠さない |
| C14 検索0件 | 語句に一致する項目がないことと修正方法を伝える | 保存データ全体が空である状態から区別する。自動の作成誘導は重複作成を促す可能性がある | R13。検索語を保持し、修正・消去・終了で回復できる |
| C15 空状態 | 一覧の集合ごとに空の意味と次の操作を示し、作成・対象変更には標準の大きいボタンを使う。見出し・説明・操作名はF03に従い、記号と操作領域の大きさを保つ | 初回作成、対象変更、下書き再開、復元は異なる文脈を持つ | R13、F07。読込中と失敗を空状態と取り違えず、作成・対象変更・閉じるへ進める |
| C16 読込表示 | List内で初期・更新・追加取得の進行を伝える。集合変更中は旧一覧を操作不可で保持し、先頭に取得中の集合を示す。中断時は進行表示を終え、44 pt以上の「読み込みを再開」を先頭に置く | 内容の文脈を保つ。全面の進行表示は更新ごとに内容を隠す負担がある。中断をエラーや0件と扱わず、同じ要求を再実行できる | R12。選択直後から完了までの中間状態を検査し、古い結果・取消・失敗で空表示を確定しない。測定できない進捗割合を示さない |
| C17 失敗と再試行 | 失敗した処理・理由・回復方法を検索前案内より先に示す。読込の再取得、コピーの失敗、コピー済みの使用記録失敗を分け、回復操作は枠付き・最小44 ptの独立したボタンにする | 探索や入力を保持したまま回復する。短時間通知だけでは必要な行動を読み逃しやすい | R10・R13。失敗した対象が分かり、「一覧を再読み込み」は再取得のみを行い、コピー・削除の再実行と誤認させない。「回数と日時を記録し直す」は本文をコピーせず、同じ操作IDの記録だけを再試行する |
| C18 完了通知 | 一覧・検索の専用UIWindowでタブバーまたはキーボードの上に左右16ptの全幅カードと読み上げ・触覚で完了を伝える。表示資格中はhostを維持し、ScopedAnimationで0.3秒の不透明度と8ptの移動を補間する。不透明なニュートラル色の背景と反転した文字色を使い、結果文を太字で優先する。削除・復元の対象名は1行で省略する。表示資格・入力透過・削除シートの扱いは[通知設計](rationale/result-notices.md)に従う | 一覧を維持して入力先への復帰を支え、背後の文字や面が透けることによる読みにくさを防ぐ。毎回の確認ダイアログは操作を中断する | R11、F02・F07・F08。起動直後のコピーを含め、成功後に2秒（削除は6秒）表示し、連続操作・期限交差・画面終了で古い状態を残さない。[通知の設計](rationale/result-notices.md)に従う |
| C19 削除取り消し | 対象名付き削除通知と見た目36pt・操作領域44ptの「元に戻す」アイコンを同じ横並びのまとまりに置く。ボタンは通知背景と反転する円形で識別し、処理中は操作を無効にする | 直前の誤操作から短い手順で回復する。期限後の復元は削除一覧が担当する | R10・R11。通知IDと対象UUIDが一致し、連打を重複実行せず、失敗には再試行を示す。連続削除と通知消去後も復元できる |

## 編集

実装：[SnippetEditor](../../app/Shared/Editing/SnippetEditor.swift)、[EditorModel](../../app/Shared/Editing/EditorModel.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C20 編集シート | 一つの作成・編集に集中し、終了時に呼出元へ戻る | 一時的な作業と利用の一覧を分ける。シート内で入力と終了条件を統一する | R09。新規・保存済みの編集・再開・共有で入力を保持する |
| C21 タイトル入力 | 本文の前に任意の識別名を入力する。常設ラベルで任意と伝え、プレースホルダーは入力例にする | F03の共通文字スタイルで一覧と同じ階層に揃える。内容の名前を先に読み取れる。必須にすると短い定型文にも命名の負担が加わる | R13・R14。空でも本文を保存でき、長い名前は折り返す |
| C22 本文入力 | 縦に伸びる標準入力欄で原文を編集する。空または空白のみの場合は本文が保存に必要なことを説明し、空の新規作成では本文にフォーカスする。空白・改行の保持とUTF-8の長さ上限の説明はC31へまとめる | F03の本文スタイルで一覧から編集への文字の変化を減らす。固定高の内側と外側でスクロールが競合する構成を避ける | R14、F09。変換、選択、末尾編集、空白・改行・Unicodeの保持を確認する |
| C23 ペースト | 本文ラベル横に「末尾にペースト」の補足と、アイコンのみの標準PasteButtonを置く | 本文へ追記する操作をその近くに置き、保存より控えめな大きさにする。OSの明示的なペーストを使う | R13、F05・F09。選択位置への挿入と末尾追記を取り違えず、44ptの領域から操作できる |
| C24 保存 | ナビゲーションバーのconfirmationActionにチェックの「保存」を置き、保存済み項目への反映を確定する | 完了位置は標準に合わせ、36ptの円形と反転色で主要操作を示す。44ptの操作領域と読み上げ名を保ち、保存不可は無効化と薄い表示で伝える | R05・R09。本文が空白のみなら無効。成功時だけ閉じ、重複保存しない |
| C25 閉じる | ナビゲーションバーのcancellationActionに×の「閉じる」を置き、下書きを保持して戻る。下部の補足でこの終了方法を案内する | 保存と同じ36ptの円形・44ptの操作領域で中断と確定を分ける。入力を捨てる意味に受け取られないよう補足する | R09・R13。最新入力の保持成功を待ち、失敗時は画面に残す |
| C26 その他メニュー | 下部バーの「…」アイコンのみの入口に本文共有と下書き破棄をまとめる。操作領域は44pt以上、読み上げ名は「その他」を保つ | 低頻度の操作を可視の入口へ収め、入力面積を確保する。確定・中断とは操作の階層を分ける | R05・R08。必要な操作を発見でき、保存済み項目の削除と下書き破棄を区別できる |
| C27 本文共有 | ShareLinkで編集中の本文をOSの共有先選択へ渡す | 標準の共有先と操作を使い、独自の共有先管理を持たない | R08、F09。本文が空なら無効。共有から入力を保持して戻れる |
| C28 下書き破棄 | 確認ダイアログで下書きの内容が元に戻らないことを伝え、除去を確定する | 保存済みを残すことを明示し、中断との意味を分ける | R10・R13。取消で入力を保ち、確定時だけ下書きを除く |
| C29 終了処理 | 入力を固定し、「保存中」「下書きを保存中」「下書きを破棄中」で進行を示し、一度だけ完了させる | 処理中の入力変更による確定対象の曖昧さを防ぐ | R12、F07。状態の説明が実行中の操作に合い、失敗時に操作可能へ戻る |
| C30 編集の失敗 | 入力の近くに理由を残し、対応可能な競合には別項目として保存する入口を示す | 入力と回復の関係を保つ。自動上書きは別の編集を失わせる可能性がある | R10・R13。失敗した保存・閉じる・破棄を区別し、画面に残る入力と再操作を案内する。別項目への保存が失敗した後も再試行の入口を残す。利用者が選んだ場合だけ別項目を作る |
| C31 終了の補足 | 入力していない画面下部に「保存」「閉じる」の結果とinfoアイコンの補足入口を示す。「空白や改行は、そのまま保存されます。」を保存後の利用案内と同じ説明部品へまとめる。入力中は画面側のバーを隠し、キーボード上の補足入口だけを示す。補足シートでUTF-8上限と下書きが残らない例外を説明し、戻ると入力先を復元する | 日常の入力を圧迫せず、保存の説明を分散させない。キーボード上と画面下部の表示条件を同じフォーカス状態から決める | R13・R19、F06・F09。表示／非表示・補足の往復・タイトル／本文の切替で重複せず、入力内容を保つ |
| C32 キーボード操作 | 標準入力と、キーボード上のアイコンのみの終了操作を使う。補足入口と終了操作は36ptの円形アイコンとしてsafe areaに沿う同じ行の両端に置き、キーボードとの間を8pt空ける。入力していないときはキーボード用の操作を出さない。背景だけをsafe area外まで広げ、入力欄と操作はキーボードを避ける | 日本語変換・選択・支援技術を尊重し、画面外のタップ以外の終了手段を持つ。OSのキーボード高に追従し、背景の見切れと操作面の密着を防ぐ | R16・R19、F04。本文の改行を保存に割り当てず、終了後も入力を保つ。ソフトウェアキーボード表示中も補足と操作が重ならない |

## 設定・復旧・製品情報

実装：[SettingsView](../../app/Nibble/Settings/SettingsView.swift)、[AboutView](../../app/Nibble/Settings/AboutView.swift)、[DeletedSnippetsView](../../app/Nibble/Library/DeletedSnippetsView.swift)、[LibraryScreen](../../app/Nibble/Library/LibraryScreen.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C34 削除一覧の入口 | 設定に区分見出しを付けず、末尾に「›」の付いた「削除した項目」の行を置き、削除一覧へ進む | 日常の再利用対象と管理を分ける。一つの入口を「保存した項目」という広い見出しで括らず、操作対象を直接示す。一覧フィルターに置く案は復旧への到達が短い一方、集合の選択肢が増える | R06、F09、G11。復元場所を初見で発見できるか評価する |
| C35 製品情報の入口 | NavigationLinkで「nibbleについて」へ進む | 説明を設定の下位階層として扱う。シートは独立作業の意味を持つ | R05。内容を予想でき、標準戻るで設定へ戻れる |
| C50 バージョン情報 | 設定の下部にCFBundleShortVersionStringとCFBundleVersionを「バージョン release (build)」で表示する。遷移や「›」、バージョン表示の区切り線は付けない | 操作項目ではなく補足情報として示す。利用中の版を設定から確認できる。ビルド時に付与された値を読み、配布版を固定文字列で誤表示しない | S04、F09、R06。インストールしたbundleと表示が一致し、狭幅でも読める |
| C36 製品情報 | 導入、基本操作、共有、ショートカット、データの扱いを一列の文章で説明する。見出しは本文と一緒にスクロールし、URLは用途ラベル付きの選択できる文字で示す | 行の区切りや固定見出しを使わず、余白とF03の共通文字階層で読み順を示す。URLも同じ本文書体に揃え、独立表示で日本語の段落幅を保つ | R03・R13・R14・R16、S05。小画面・固定した標準文字で長文を省略せず読み切れ、端末内保存と削除の意味を理解できる |
| C37 削除済み行 | 名前と要約で対象を識別し、対象名を読み上げる復元と可視メニューを置く。内容は編集ボタンにしない | コピー・編集の対象から復元対象へ操作を切り替える | R04・R11。対象名が分かり、同じUUIDと原文で復元できる |
| C38 完全削除 | 対象名、回復不能であること、関連下書きの除去を確認する | 直接実行より対象確認と誤操作の防止を優先する | R10。対象と結果を理解し、取消で維持できる |
| C39 削除一覧の検索・終了 | 削除済みだけを検索し、閉じるで設定へ戻る | 復旧と通常検索の対象範囲を分ける | R02・R09。0件・失敗・復元後に回復でき、新規作成を表示しない |

## システムと共有拡張

実装：[AppRootView](../../app/Nibble/Navigation/AppRootView.swift)、[SnippetEditor](../../app/Shared/Editing/SnippetEditor.swift)、[ShareViewController](../../app/NibbleShare/ShareViewController.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C40 非アクティブ表示 | アプリ切替時に本文を内容のない面と製品名で覆う | 切替画面への本文の露出を抑えるため、内容より前面へ置く | 製品の保護方針。シート、OSの画面取得時点、共有拡張での適用条件を評価する |
| C41 共有取込 | テキストまたはURLを1件受け取り、下書きを作って共通編集へ渡す。「共有から保存」の見出しと確認の案内を入力前に置き、「閉じる」で共有元へ戻ることを補足する | 内容を確認してから保存し、本体と同じ編集契約を使う。取得していない共有元名やページ情報は表示しない | C20〜C32。遅延・取消・取得失敗で状態と終了方法を理解できる |
| C42 取込失敗 | 編集開始前の問題を標準alertで示し、閉じるで共有元へ戻る | 入力可能な状態と取得失敗を区別する | R10・R13。非対応形式、空白のみ、長さ超過、取得・保存先の問題を分ける。共有元での修正・再共有を案内し、編集開始前に「保存」を押すよう指示しない |
| C43 OS所有の要素 | ステータスバー、ホームインジケータ、キーボード、検索の標準操作、シート外形、共有先選択 | OSの学習済みの操作と適応を使い、独自描画の保守を減らす | R03・R09・R19。セーフエリアを守り、必要なOS操作を妨げない |

## 説明の動き

実装：[AboutIllustration](../../app/Nibble/Presentation/AboutIllustration.swift)、[KeyboardIllustration](../../app/Nibble/Presentation/KeyboardIllustration.swift)、[RiveCanvas](../../app/Packages/RivePresentation/Sources/RivePresentation/RiveCanvas.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C44 利用の説明イラスト | メモ画面の文章を選び、複製をnibbleへ保存する図と説明。本文幅で自動ループを表示する。周期と再生方針は演出設計に従う | 導入文の後に置き、元の文章・複製・保存先の関係を伝える。自動ループは途中からの閲覧に対応し、再生操作を不要にする。隣接するTextが説明と読み上げを担う | [演出設計](../architecture/presentation.md)、S05・F08、R20・G16。複数周期、再生ボタンなし、不可視時の停止、復帰、Reduce Motion、配色、読込失敗時の説明と再試行を確認する。理解度と読書への影響は利用者評価の対象 |

## スニペットキーボード

実装：[KeyboardView](../../app/NibbleKeyboard/KeyboardView.swift)、[KeyboardViewController](../../app/NibbleKeyboard/KeyboardViewController.swift)、[KeyboardGuideView](../../app/Nibble/Settings/KeyboardGuideView.swift)。

| ID・部品 | 責務と構成 | 配置理由と制約 | 根拠・確認条件 |
| --- | --- | --- | --- |
| C45 キーボードの対象選択 | 上部にコンパクトな「すべて／ピン留め」と更新を配置する。各タップ領域は44×44pt以上 | 排他的な集合をまとめ、一覧に使う高さを確保する。全幅Pickerに比べて更新と同じ段へ収めやすい | S09、技術仕様0005。選択状態、狭幅、OS文字設定変更時の固定表示、集合切替後の先頭ページと読込状態を確認 |
| C46 キーボードの項目と全文 | 一つの角丸リストと区切り線、タイトルと1行プレビュー、小さいピン。行の主領域は入力、独立した「…」は全文・戻る・ピン・コピー・強調した入力を持つ詳細を開く | タイトルの横幅と一覧の密度を優先する。個別カードやタイルは行間・列分割に面積を使う。詳細は入力先との位置関係を保つためキーボード内に置く | S09。1タップ1挿入、「…」で0挿入、長文末尾、戻る位置、ピンと空状態、原文・権限・連打を確認 |
| C47 キーボードの下部操作 | nibble表示と結果文を同じ領域で切り替え、成功表示と挿入行背景を2秒で消す。必要なページ操作、閉じる、OS要求時の地球儀を置く | 一覧の面積を保ち、ダイアログによる操作中断を避ける。失敗・権限案内には自動消去の期限を設けず、結果文を読み上げる。失敗・権限案内は行数で切らず、長文はスクロールして読める | S09。操作完了後の表示、通知IDごとの期限、ページ境界、地球儀、狭幅・横向き、文字設定変更時の固定表示、権限変更を確認 |
| C48 キーボード利用案内 | 本体の設定内の入口と、追加手順・全文操作・権限・入力先の制約を説明する文章 | 設定作業と入力作業を分け、長い説明を読む領域を本体に確保する | S10。全段落の可読性、標準の戻る、コピーとピン更新にだけフルアクセスが必要なこと、入力先の反映確認、実際のiOS設定名の理解 |
| C49 キーボード入力の説明イラスト | 入力欄とキーボードを上下に置き、切替・行の選択・本文挿入を自動ループで示す。図は操作を受けず、隣接Textが原文保持を含む意味と読み上げを担う | 導入文と追加手順の間に置き、設定操作の前に用途を示す。構図・比率・周期・結果保持の理由は[演出設計](../architecture/presentation.md#表現と配置の理由)に定める | S10、F03・F08・F10、R18・R19・R20、G15・G16。小画面、ライト/ダーク、複数周期、不可視・離脱・非active時の停止と復帰、配色変更、固定演出、失敗時の説明と再読み込みを確認する |

Keyboardの権限と操作契約は[設計](../architecture/keyboard.md)に従う。表示値は実装を正本とし、上記の理由と評価条件に照合する。
