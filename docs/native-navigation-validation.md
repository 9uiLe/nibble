# 一覧・設定・検索の検証

## 対象と環境

標準タブの「一覧」「設定」「検索」、上部の3フィルター、新規作成・コピーの左右設定、共通のリスト背景を対象とする。採用する機能と責務は[製品設計](decisions/0002-mvp-app.md#画面とフィードバック)、再現手順は[MVP手順](mvp.md)に定義する。

| 条件 | 値 |
| --- | --- |
| 実施日 | 2026-09-17 JST |
| 製品・テスト・driverのソース | `f274f4ef605b62f9b6f61ecb73a99dcece33bea6`。文書のみを変更したコミットとの再利用は入力hashで照合する |
| 変更前の画面比較 | `856b98f573f39a0ad405a52b4c6cba5c99822a73` |
| 端末 | 専用iPhone 17 Pro Simulator、402×874 pt、iOS 26.5（23F77） |
| UDID | `FE82EC12-56CF-4DEC-A6DC-9DE61A5C4AA2` |
| ツール | Xcode 26.5（17F42）、Swift 6.3.2、macOS 26.2 arm64、Nix固定のsim-use 0.14.0 |
| 識別子 | 本体`nibble.9uiLe.com`、共有拡張`nibble.9uiLe.com.share`、App Group `group.nibble.9uiLe.com` |
| データと構成 | ダミーの日本語・空白・改行・Unicode、Release。既存項目を保持 |

## 製品テストと基本操作

Release製品テスト47件、パラメーター展開後49実行が成功した。失敗・skipはない。保存層・下書き・待機可能なモデル操作・タスク所有・表示比較に加え、一覧の区分、各フィルターの対象、検索モデルの独立性、取得上限と最新要求の反映を検査した。

標準UI driver `scripts/check-mvp-ui.py`でも以下の操作と結果の照合に成功した。

| 契約 | 確認した結果 |
| --- | --- |
| 作成と下書き | ＋から入力して閉じ、下書きフィルターから再開して保存できる。保存後は下書きから外れる |
| 検索 | 日本語で保存済み項目を検索できる。入力中に見出しを保持し、新規作成を隠す。検索確定でキーボードを閉じる |
| フィルターと独立性 | ピン解除で対象がピン留めから消え、全保存済み項目の検索では見つかる。検索0件でも一覧は絞り込まれない |
| 編集とコピー | 編集後も同じUUIDを保持。コピーのUTF-8は空白・改行・Unicodeを含む本文と完全一致 |
| 削除と回復 | 削除・Undo、削除一覧の検索0件・絞り込み・復元を操作。復元は同じUUIDと本文を保持 |
| 破棄 | 下書きを破棄しても保存済み本文を保持する |
| 設定と配置 | 設定から製品情報へ移動。新規作成・コピーの左右配置、再起動後の保持、右側への復元を確認 |

## 外観と媒体

画像は1206×2622 pxの原本を確認した。基本操作runのライト画像6枚では、ナビゲーションバーの左見出し、3フィルター、下書き、左右の操作ボタン、標準検索とキーボード、設定の共通背景を確認した。

背景の補足runは同じ製品入力を持つ。標準文字で一覧・下書き・検索・設定・製品情報・削除一覧のライトとダーク計12画面を確認し、行・見出し・余白の色が一致した。表示設定はlight / large / contrast disabledへ復元済み。詳細は[リストの背景色](navigation-header-validation.md#リストの背景色)に記載する。

| run ID | 結果・確認した媒体 |
| --- | --- |
| `20260917T023506Z-test-9be927` | Releaseテスト47件成功。xcresultと集計JSON |
| `20260917T023811Z-mvp-ui-b8ba3c` | Release基本操作成功。`finished.png`、`draft-filter.png`、`left-library.png`、`right-library.png`、`search-keyboard.png`、`settings.png`。録画182.645秒の8.5283秒・91.3117秒・164.6167秒 |
| `20260917T014459Z-list-background-1976de` | Release外観確認成功。ライト・ダーク12枚。録画67.7秒の2.2167秒・33.815秒・60.8567秒 |
| `20260916T155944Z-navigation-before-8bcede` | 比較元のReleaseビルド・起動・撮影成功。`library.png` |

runは1回の実行記録で、`artifacts/ios/<run ID>/`にソース・端末・コマンド・成否・媒体hashを保存する。目視範囲と添付の閲覧条件は`review.json`で管理する。録画の確認は記載した静止フレームの抽出で、全編の連続再生ではない。比較元と変更後はダミーデータの件数が異なるため、画像比較を性能測定には使わない。

## 共通検査と配布

共通検査は`nix flake check --no-update-lock-file --print-build-logs`を使う。対象はNix・workflow・文書・Swift規約・Python回帰テスト86件で、実行ログを`artifacts/native-design-final-checks.log`へ保存する。GitHub ActionsはUbuntuで同じ共通検査とPR本文・コミット表の照合を行い、iOSの実行結果を代替しない。

TestFlight 0.1.0（202609170151）は上記の製品ソースでarchive・アップロードに成功した。公開manifestの`completed`はtrue、本体と共有拡張の識別子・最低OS 26.0・SDK 26.5・暗号化申告を確認した。Apple側の処理、本人用グループの配信、実機の動作は配布担当者が確認する。認証情報と保護された配布ログは参照しない。

## 評価範囲と制約

- 最終ソースのUI runは標準文字・縦画面のiPhone 17 Proを対象とする。最大文字とコントラスト強調は[フィルター](library-filters-validation.md)・[検索](search-layout-validation.md)に記したソースでの観測であり、最終ソースの全画面をその条件で再評価した結果ではない。
- 日本語入力はペースト。IME変換、VoiceOverの音声、Reduce Motion切り替え、RTL、小画面、横向き、iPad、実機、共有拡張の操作はこのソースで未評価。
- 起動・入力・タブ切り替え・描画・メモリの定量比較は未実施。保存済み項目は100件単位で取得し、ページの区分は要約を1回走査する。これらの構造と操作成功だけから性能改善を主張しない。
- 保存形式と外部依存のバージョンはこのUI構成で変更していない。Safari共有・ファイル保護・実機配布の受け入れ条件は、それぞれの検証記録で管理する。
