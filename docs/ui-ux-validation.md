# 操作・識別・回復のUI検証

## フィルターと取得結果の整合性

対象ソース：`c57a179c39fc5a110faa339ab23dc6d270f736cd`。iOS 26.5（23F77）、Xcode 26.5、Releaseで評価した。取得条件と結果を一組で扱う契約はF07・C15〜C17・S01に定義する。

原因は、フィルターの選択変更後、UIの取得処理が始まるまで前の結果と完了フラグが残り、異なる集合の空配列を0件と判定したことだった。選択時点で未完了を導出し、表示用の区分と結果を同時に反映する。待機中は旧一覧を操作不可で保持し、別要求の失敗を表示しない。

| 確認 | run / 条件 | 観測 |
| --- | --- | --- |
| 回帰テスト | `20260917T094444Z-test-e8ceda`、iPhone 17 Pro | 58テスト、パラメータ展開61実行、失敗・skipなし |
| フィルター切替 | `20260917T094441Z-filter-transitions-50e83f`、iPhone SE | 下書き→ピン留め→すべてを3周。各集合にダミー項目を用意し、9回とも選択に合う内容を確認 |
| 基本操作 | `20260917T094616Z-mvp-ui-937a2d`、iPhone 17 Pro | 作成・保持・再開・保存・検索・編集・コピー・ピン・削除・Undo・破棄・復元・左右設定と再起動保持が成功 |

回帰テストは、選択直後でUIの非同期開始前、旧取得の成功・失敗が新選択の開始前に返る順序、新結果が先に返る順序、失敗から別集合への変更、取得済み0件、追加取得、正規等価な検索語を扱う。読取の完了をテスト側で制御し、時間待ちや偶然の実行順に依存しない。操作の開始と寿命は製品のTasking所有者を通す。

切替録画は13.068秒、保存された全289フレームをApple Visionで文字認識した。空表示の文言は検出せず、全フレームにダミー項目名が存在した。文字認識は誤認識の可能性があり、未記録の表示フレームや全端末条件の保証にはならない。PNG3点と0・5.96・12.312秒の抽出フレームを目視した。連続全編再生は実施していない。

初期再現run `20260917T093257Z-test-acc821`では選択直後の未読込判定が3か所で失敗した。別の1か所は、検索に下書きが含まれないというテストの誤った前提だったため修正した。run `20260917T093625Z-test-55eba2`は編集途中の代入先誤りによるビルド失敗で、成功証跡に含めない。これらと、検索語の判定調整前のrunは残している。

取得SQL、ページ上限、スキーマ、スケジューラーは変更していない。保持する結果は直近1ページで、フィルター別cacheは追加しない。同じ17 Pro向けRelease Simulator条件のアプリ通常ファイル合計は7,183,344→7,253,840 bytes（+70,496 bytes、約0.98%、test bundle/dSYM除外）。描画・応答速度の改善率や実機性能は測定していない。

最終ソースの共通検査はNixの6 checkが成功した。UI証跡の開始・終了時の入力、媒体hash、対象ソースを照合し、添付先の閲覧確認はPRと各runのレビュー記録に保持する。

## 操作・識別・回復の評価条件

評価日：2026-09-17。製品ソースは`cbcdb9f34d7d391d6e06206e33525023ed8e311a`。採用仕様は[UIの判断記録](design/decisions/0001-library-actions.md)、画面と部品の契約は[設計台帳](design/README.md)に定義する。

比較元は`619361e20244bb0e148844ac3397bcb5d86042a9`。Xcode 26.5（17F42）、Swift 6.3.2、macOS 26.2 arm64を使う。最低対応OSは26.0、実行対象はiOS 26.5 Simulator（23F77）。

| 端末 | UDID | 条件 |
| --- | --- | --- |
| iPhone 17 Pro | `FE82EC12-56CF-4DEC-A6DC-9DE61A5C4AA2` | 402×874 pt、標準文字と最大アクセシビリティ文字 |
| iPhone SE（第3世代） | `AED98CBE-D67C-44EB-8B9D-F94719466B1A` | 375×667 pt、狭幅の内容・操作への到達 |

ダミーの日本語・空白・改行・結合文字・絵文字を使う。追加した20件の下書きと復旧用の1件はUUIDで追跡して片付け、専用Simulatorの既存項目を保持する。外観・文字サイズ・コントラスト設定は実行前の値へ戻す。

## Releaseテスト

run `20260917T090457Z-test-de4336`で54テストが成功した。パラメータ展開後56実行、失敗・skipなし。本体と共有拡張をReleaseでビルドしている。

確認した契約は、原文とUUID、保存・検索・復元、下書きの順序と上限・識別情報、完了を待つ操作、所有者の重複・キャンセル、空白検索中の失敗保持、読込失敗と操作失敗の分離、連続削除と取り消し対象、本文必須の説明、拡大行の高さ、Viewの表示入力と環境変更である。

色のテストはライト・ダークと通常・高コントラストの4組について、独自アクセントと背景の比が4.5:1以上で、高コントラストの比が通常より大きいことを確認した。半透明素材・システムの補助文字・全画面の可読性をこの計算だけで保証しない。

## 画面と操作の確認

| run | 条件 | 結果 |
| --- | --- | --- |
| `20260917T080523Z-ux-before-792bda` | 比較元、iPhone 17 Pro、標準文字 | 一覧・下書き・編集を撮影 |
| `20260917T090317Z-ux-adaptation-d0a916` | 最終製品ソース、iPhone SE | 下記の追加導線が成功。空状態の作成と＋の非重複も確認 |
| `20260917T090536Z-ux-adaptation-48e479` | 最終製品ソース、iPhone 17 Pro | 下記の追加導線が成功 |
| `20260917T090651Z-mvp-ui-486966` | 最終製品ソース、iPhone 17 Pro、標準製品driver | 作成・保持・再開・保存・検索・編集・コピー・ピン・削除・Undo・破棄・復元・設定が成功 |

追加導線は、20件から直近3件への限定、全下書きへの移動、無題の識別と再開、可視メニュー、対象名付きUndo、最大文字の選択フィルターと縦の操作配置、コピー本文の完全一致、ダークとコントラスト強調、本文必須の説明、完全削除の取消と復元、空白検索での失敗表示、明示した再読込による回復である。

最大文字の小型画面では内容全体と操作を一度に収めず、スクロールで操作を作成ボタンの上まで動かせることを確認した。下書き全件への入口、Undo、復元、再読み込みはAXの操作領域が44×44 pt以上であることを検査した。空の一覧の作成ボタンは高さ51 ptだった。

比較元と最終ソースは各runの開始・終了・コミットの入力と照合した。補助driverは実行時の自身のhashをmanifestへ保存する。PNG原本と録画の3抽出フレームを確認し、連続全編再生は実施していない。公開媒体のURLとブラウザーの閲覧条件は各runのreview.jsonとPRへ記録する。比較画像のダミーデータ件数は一致しないため、同条件の密度や作業時間の比較には使わない。

## 下書き取得の比較

同じmacOS・Nix Python・SQLite 3.51.2で、変更前後の製品SQLを比較した。0・20・1,000・10,000件、各本文1,700文字、warmなメモリDB、30回を交互の順序で測定した。新実装は更新日時とIDの索引を持つ。表は中央値、単位はms。

| 下書き件数 | 変更前：全要約 | 変更後：一覧用4件 | 変更後：下書きの先頭101件 |
| --- | --- | --- | --- |
| 0 | 0.001 | 0.004 | 0.004 |
| 20 | 0.015 | 0.013 | 0.040 |
| 1,000 | 0.697 | 0.032 | 0.193 |
| 10,000 | 9.572 | 0.243 | 0.217 |

一覧には3件を表示し、4件目は続きの有無の判定に使う。下書きの専用フィルターでは100件を表示し、101件目で続きの有無を判断する。変更後は本文の要約と日時を含むため、返す情報も異なる。索引なしでの1万件・先頭101件は中央値20.378 msであり、件数制限だけでは並べ替えの負担を抑えられなかった。索引の初回作成は1万件で5.552 msだった。

測定にはSwiftの値への変換、実ファイルのI/O、画面描画、入力追従を含めない。少量データの各経路が一律に高速化したとは評価しない。採用理由は必要な要約の取得量と並べ替えを抑えることであり、アプリ全体の速度改善とは区別する。生の試行値・SQL・ソースとdriverのhashはGit管理外の`artifacts/draft-query-timing.json`に保存した。

## 共通の自動検査

`nix flake check --no-update-lock-file --print-build-logs`の6 checkが成功した。hostの回帰93件、共通UI設計Moduleの回帰35件（変更のないderivationの成功cache）、Swift規約31ソース、Markdown 53件・ローカルリンク540件・Swift例2件を照合した。設計の照合対象は61入力、C43・S8・F9・R19・G12のIDで、未確認変更と参照の不整合はない。

標準製品driverでは日本語・空白・改行・結合文字・絵文字を含むコピーのUTF-8完全一致、編集・Undo・削除一覧からの復元で同じUUID、下書き破棄後の保存済み原文維持、左右の設定と再起動後の保持を確認した。設計IDの整合と操作assertionの成功は、利用者比較の結果を含まない。

## ビルドサイズ

同じiPhone 17 Pro向けRelease Simulator buildの通常ファイルを合計した。test bundleとそのdSYM（パスに`.xctest`を含むもの）は双方から除いた。

| 対象 | 比較元 | 最終製品ソース |
| --- | --- | --- |
| Nibble.app全体 | 6,386,000 bytes | 7,183,344 bytes |
| 本体実行ファイル | 4,323,168 bytes | 5,053,472 bytes |
| 共有拡張実行ファイル | 2,009,568 bytes | 2,076,608 bytes |

全体は797,344 bytes（約12.5%）増えた。状態表示、操作入口、文字拡大への適応を加えた実装の費用として記録する。App Storeのdownload size、端末用IPA、実行中メモリの値ではない。

## 確認中に検出した問題

- 回復・復元などでButtonの外側に最小寸法を指定しても、AXに報告されるボタン本体は20×18 ptや151×34 ptだった。反復操作はラベル内部で領域を確保し、回復と空状態は標準のlargeサイズを採用した。確認対象の実寸を補助driverの合格条件に加えた。
- run `20260917T085114Z-ux-adaptation-2f783b`では親のエラー識別子が回復ボタンに引き継がれた。識別子を見出しとボタンに分け、空白検索でも回復操作を独立して参照できる構成にした。
- 補助driverのメッセージなし例外が失敗理由を空にするケースを検出した。製品driverは例外の表現を保存し、空の例外でも失敗終了する回帰テストを追加した。exit 1のrun `20260917T084434Z-ux-adaptation-9a9e8d`は成功した証跡に使用しない。
- 小型画面でシートや確認を閉じる途中に、画面読取が一時的に空になるケースがあった。補助driverは対象アプリの生存を確認したまま、対象アプリの空の成功応答に限り最大5回の読取を行う。非ゼロ終了や別のエラーを無視しない。


- run `20260917T082515Z-mvp-ui-71d910`で、見出しの固有幅を確保しない変更により「一覧」が省略された。短いルート見出しの幅を確保する実装を採用した。
- run `20260917T083058Z-ux-adaptation-a33019`は、simctlのApp Group直接指定が失敗した。列挙されたGroup IDとパスの対応を使うdriverへ修正した。製品の保存失敗ではない。
- `d6b0b27c9882e239d56edf9ddefcd45a1ccddbda`のSE run `20260917T083658Z-ux-adaptation-98f1c0`は、最大文字で画面外のメニューを待つ確認が失敗した。ピン留め集合の重複見出しを省く実装と、スクロールして操作へ到達する確認を採用した。

失敗runは成功runで上書きせず保存する。最終ソースの実行結果と、途中の観測を区別する。

## 評価範囲の制約

初見の利用者による操作発見性・所要時間の比較、VoiceOver音声での操作、スイッチ操作、横向き、Reduce Motionの再確認、日本語IME変換、外部アプリから共有拡張へ入る一連の操作、実機の描画・メモリ・電力は本記録で評価していない。AX情報の名前・対象・配置の検査をVoiceOverの実操作成功と扱わない。

共有元のデータ取得中の進行・取消は監査G12の未採用課題として残す。編集シートは、最新入力の保持結果を確認して閉じる契約に従い、明示終了を使う。スワイプ終了を求める利用者の期待との比較は未実施である。製品全体の受け入れと、この変更の検証結果を区別する。
